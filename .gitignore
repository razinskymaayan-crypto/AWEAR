# Secrets — never commit
.env
.env.*
!.env.example
*.key
google_credentials.json
google_token.json
client_secret_*.json

# Python virtual envs
.venv/
venv/
venv312/

# Python cache
__pycache__/
*.pyc

# OS
.DS_Store
.env
venv312/
.tg_chat
.tg_inbox

# Dashboard — regenerated on every request, not source of truth
agents/data/dashboard_data.json

# graphify — generated knowledge graph, regenerate with `graphify update .`
graphify-out/

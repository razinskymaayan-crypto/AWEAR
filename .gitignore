# Secrets — never commit
.env
.env.*
*.key
google_credentials.json
google_token.json
client_secret_*.json

# Python virtual envs
.venv/
venv/
venv312/

# Python cache
__pycache__/
*.pyc

# Telegram local state
.tg_chat
.tg_inbox

# Runtime data — regenerated, not source of truth
data/awear.db
agents/data/dashboard_data.json
.intel_run_count

# Local tools — binaries, not source
tools/

# graphify knowledge-graph output (generated locally)
graphify-out/

# Node / Capacitor (generated; ios/ is regenerable via `npx cap add ios`)
node_modules/
ios/

# OS
.DS_Store

# Raw camera originals — app serves converted JPGs in static/img/users/
static/img/closet/*.HEIC
static/img/closet/*pic/

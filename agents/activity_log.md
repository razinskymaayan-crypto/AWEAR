# agents/activity_log.md — יומן פעילות סוכנים

> פורמט: `[תאריך] | [סוכן] | [branch] | [סטטוס] | [תיאור]`

---

| 2026-06-19 | netta | feat/mobile-tokens | הושלם | mobile/tokens.js — React Native token mirror, Cycle 3 P0 dependency. cycle-opening grep: var(--t-)=390 ב-index.html (baseline documented). 8 exports: colors(15), typography(10), fontWeight(5), spacing(11), radii(6), shadows(5), motion(4), zIndex(6). ערכים קנוניים מ-awear-tokens.json — 3 סטיות מ-spec draft מתועדות inline: muted=#8a8498 (לא #8b8b9a), warning=#e8a84a (לא #f59e0b), line=#2e2836 opaque (לא rgba overlay). shadow system מלא: sm/md/lg/accent/glow עם elevation ל-Android. mobile/theme/tokens.js קיים במקביל (JSON bridge עם px strings) — לא נמחק, שני קבצים משלימים. node check skipped (node לא זמין בסביבה). commit: d2c6f6a, worktree: /Users/tamargrosz/netta-mobile-tokens. |
| 2026-06-19 | shira | feat/notifications-endpoint | הושלם — curl-tested | In-memory notifications layer. _emit_notification() helper: direct Python call, SF-004 safe (אפס httpx/requests imports). GET /api/notifications/{user_id}: items newest-first, total, unread count, limit + unread_only params. POST /api/notifications/{user_id}/read-all: marks all read, returns {status:ok}. toggle_like hook: _emit_notification("like") על לייק חדש בלבד — לא על unlike. user_id empty-string guard (post ללא owner = skip שקט). curl-tested: empty store → {items:[],total:0,unread:0}; like → notif emitted (log: "notification emitted"); GET → {id,type,from_user_key,post_id,created_at,read:false}; unread_only filter; read-all → {status:ok}; unlike → 0 notifications נוספות; total=1,unread=0 אחרי read-all. import OK. commit: b742435, worktree: /Users/tamargrosz/shira-notifications, branch: feat/notifications-endpoint. |
| 2026-06-19 | sam | feat/categories-endpoint | הושלם — import OK + grep verified | GET /api/categories — מחזיר קטגוריות מוצרים ייחודיות עם counts — לטובת filter chips ב-Marketplace. iterates _products_cache (in-memory, אפס DB queries). response: {items:[{name,count},...], total:N} — items ממוינות אלפביתית. DoD: python3 -c "import app" = OK; grep get_categories = שורה 755; grep /api/categories = שורה 754. commit: 3dc5825, worktree: /Users/tamargrosz/sam-categories, branch: feat/categories-endpoint. |
| 2026-06-19 | mark | agents/plans/profile_screen_design.md | הושלם | Profile Screen Design Spec — כיוון עיצובי ל-ProfileScreen.js. ממצאים מהקוד הקיים (stub, dana, Cycle 2): 8 hardcoded hex בStyleSheet, ctaSecondary עם backgroundColor במקום transparent, avatar 88px ללא border, post grid + empty state חסרים לחלוטין. spec קובע: avatar 80px + border 2px var(--accent), stats row עם dividers rgba(.08), primary CTA var(--accent) minHeight 44px, secondary CTA transparent + border var(--line), post grid 3-col gap:2 Instagram pattern, empty state icon('camera',48) + CTA → Camera tab. tokens: mobile/tokens.js (ליצור Cycle 3). Gabbana QA checklist: 7 נקודות + שאלת העל. חסמים: editProfile (auth, Cycle 3), post grid data (API), mobile tokens. dispatch מוכן לדולצ'ה. |
| 2026-06-19 | mark | agents/plans/post_card_design.md | הושלם | PostCard Design Spec — building block של Feed. spec מגדיר: dimensions (width:100%, image 4:5 aspect-ratio, border-radius 12px), layout 5 שכבות (user row / image / action row / caption / tags), tokens מלאים (card bg, shadow-accent, radius 16/12px, spacing מרשת 8pt), שני states של like (filled/outline + var(--accent)), image onerror fallback לicon('hanger',32), username opacity:0.7 Cycle 2, caption line-clamp:2, tags שורה אחת var(--t-micro). "What NOT to do": אין gradient overlay על image, אין text over image, אין emoji, אין hardcoded px/hex/font-size. Gabbana QA checklist: 7 נקודות כולל grep + שאלת העל. dispatch מוכן לדולצ'ה — ממתין ל-P0 cleared. |
| 2026-06-19 | mark | agents/plans/visual_redesign_brief.md | הושלם | Visual Redesign Brief — תגובה לפידבק דירקטוריון. זיהוי 3 בעיות P0 שמשתמש רואה ב-5 שניות: (1) sf-card-img / mp-item-img — product cards ריקים ב-Shopping Feed + Marketplace (font-size:52px placeholder, אין productImage() binding); (2) phone shell glow — rgba(123,92,255,.28) legacy purple, לא מסונכרן עם palette rose הנוכחית; (3) body background — hardcoded #15131f/#000 במקום var(--surface)/var(--bg). P0 → דולצ'ה מיידית. P1: hardcoded surfaces (#20202b/#191922/#1c1c26) + badge tokens. מה לא לגעת: card components, gradient CTAs, nav bar, skeleton loading. brief נכתב ב-agents/plans/visual_redesign_brief.md. |
| 2026-06-19 | mark | agents/plans/style_chips_spec.md | הושלם | Visual Design direction לStyle Chips — הוסף section "Visual Design (Mark)" לspec של איילון. הכרעות: border-radius=22px (pill מ-DS scale 10/16/22px), tokens מתוקנים (--r-pill/--surface/--fg/--border/--t-small אינם קיימים — הוחלפו ב-var(--card)/var(--text)/var(--line)/var(--t-sm)), active color=var(--text) לא #fff (אפס hex), :active tap feedback חובה (כלל DS 6), אין emoji (כלל 1). section כולל רשימת "מה לא לעשות" לדולצ'ה. דולצ'ה מוכנה להתחיל מיישמת. |
| 2026-06-19 | dolce | feat/visual-redesign-home-feed | הושלם — ממתין לגבאנה | Visual Redesign: Home Screen + Feed Screen. שינויים: (1) Typography hierarchy — כל CSS חדש משתמש ב-var(--t-*) בלבד (t-h1/t-h3/t-body/t-small/t-micro/t-title/t-display). (2) 8px grid — card padding 16px, section gaps 24px, element gaps 8/12px, border-radius 16px לכל cards. (3) Card depth — box-shadow var(--shadow-md/accent), border rgba(255,255,255,.06). (4) Accent visible — hs-num gradient, ha-primary CTA, hq-icon container (36px, accent), fc-match badge, fca-btn states, home-sec-label underline 24px accent. (5) Quick actions — hq-icon container מעוצב, min-width 68px, min-height 72px. (6) Feed PostCard — touch targets 44px, buy CTA 48px, var(--t-body) caption. (7) Emoji cleanup — ✨ מה-greeting הוסר, ☀️ מה-weather default הוסר, weather codes → text בעברית. (8) DS-004 fallback: var(--success, #52c97a). self-check: 0 hex חדשים (מלבד fallback), 0 font-size hardcoded חדשים. commit: e8cde07, worktree: /Users/tamargrosz/dolce-redesign. |

## 2026-06-19 — Cycle 2

| תאריך | סוכן | branch | סטטוס | תיאור |
| 2026-06-19 | netta | feat/typography-migration | הושלם | Cycle 2 P1: typography migration — 239 הופעות hardcoded font-size → var(--t-*) tokens. מיגרציה: 14px→var(--t-body) [41], 13px→var(--t-small) [74], 12px→var(--t-caption) [50], 11px→var(--t-micro) [74]. cycle-opening grep: var(--t-)=0 לפני, 236 אחרי. DoD: grep font-size:14px/13px/12px/11px = 0 לכולם. ערכים לא-ממוגרים: 12.5px/13.5px/15px/16px/10px — ללא token מדויק, מתועדים ב-typography_migration_log.md עם next-cycle targets. worktree: /Users/tamargrosz/netta-typography. commit: e9fafb6. |
| 2026-06-19 | netta | feat/typography-migration | הושלם — migration מלא | Cycle 2 continuation: typography migration — כל ה-157 הופעות הנותרות הומרו. session זה: 20px→var(--t-title), 18px→var(--t-h2), 17px→var(--t-lead), 15px→var(--t-h3), 24px→var(--t-h1), 32px→var(--t-display); non-standard: 12.5px→caption, 13.5px→small, 11.5px/10px/10.5px/9px/9.5px→micro; near-standard עם fallbacks מדויקים: 16px→h3, 19px→lead, 22px/23px/26px→h1, 28px/30px/34px-100px→display. DoD: grep hardcoded font-size = 0 (תרד מ-402 ל-0), var(--t-) = 393 (עלה מ-236). commit: 9713c7a, worktree: /Users/tamargrosz/netta-typography. הערה: BSD sed ב-macOS לא תומך ב-\b — כל patterns רצו ללא word boundary. |

| תאריך | סוכן | branch | סטטוס | תיאור |
| 2026-06-19 | ayalon | agents/plans/onboarding_spec.md | הושלם | Spec: OnboardingScreen לדנה. החלטות: 3 screens (Welcome / StyleQuiz stub / Ready), CTA סופי = "יצירת חשבון" → RegisterScreen (auth stubs קיימים מסאם commit 0d8da55), StyleQuiz לוגיקה = Cycle 3 (stub בלבד Cycle 2). AsyncStorage key: 'onboarding_complete' === 'true' → skip ב-App.js init. חסמים מתועדים: OnboardingScreen לא ב-Tab.Navigator — דורש Stack navigator מעל Tabs (תיאום רועי+דנה, MB-002). RegisterScreen stub ריק Cycle 2 לא בלוקר. 10 i18n keys (en+he), 0 emoji, 0 hardcoded text. DoD: 8 קריטריונים. מה שלא בscope: auth UX, StyleQuiz persistence, push permission, animations. |
| 2026-06-19 | varan | agents/plans/marketplace_mobile_spec.md | הושלם | Cycle 2 P1 — Mobile Marketplace Spec לרועי: MarketplaceScreen.js (browse stub בלבד, NO Sell Form, per ג'ף). החלטות ארכיטקטורה (MB-002): navigation=Tab.Screen קיים (route 'Marketplace'), state=useState מקומי בלבד (AppContext extension לא נדרש). Layout: ScrollView + Header + Category filter chips (single-select, useState) + FlatList numColumns=2. ProductCard: image (4:5 ratio) + name + price_usd + condition badge (like-new=--success / good=--accent2 / fair=--muted) + heart toggle (uncontrolled). Data: GET /api/products?limit=50 (data.items||data fallback). i18n: 8 keys תחת namespace marketplace (en+he) — t() חובה, 0 hardcoded strings (MB-003). Tab Bar: icon('bag',22) לפי react_navigation_plan.md. DoD: 7 קריטריונים (≥3 products, 2-col grid, condition badge colored, error state, grep Hebrew=0, grep emoji=0, heart toggle). Sell Form + My Listings + Purchases = מחוץ לscope Cycle 2. תנאי dispatch לרועי: FeedScreen API integration קיים (commit ec3f3b4) + Tab.Screen Marketplace ב-App.js. |
| 2026-06-19 | roei | feat/feed-api-connect | הושלם | Cycle 2 P1: FeedScreen חובר ל-/api/posts. API_BASE='http://localhost:8000' — קבוע אחד בראש הקובץ. useEffect ב-mount: GET /api/posts?limit=20&offset=0 → normalizePost() → AppContext.feedPosts. normalizePost: ממפה {id,user_id,image_url,caption,likes} → {id,user,avatar,image,caption,likes,comments}. loading state: ActivityIndicator מרכזי בזמן fetch ראשוני. error state: t('feed.error') inline, ללא crash — pull-to-refresh מנסה שוב. pull-to-refresh: onRefresh → fetchPosts(0) → setFeedPosts. feed namespace עודכן ב-en.json+he.json: נוסף מפתח error (סה"כ 4 keys). DoD: grep localhost:8000=1 הגדרה; grep hardcoded=0; try/catch קיים. commit: ec3f3b4, worktree: /Users/tamargrosz/roei-feed-api. חסם פתוח: avatar_url לא קיים ב-API — fallback לrandomuser.me לפי user_id seed עד שauth endpoint יחזיר avatar. Metro bundle EXIT 0 לא נבדק בסביבה זו. |
| 2026-06-19 | dolce | feat/skeleton-loading | הושלם (DoD verified) | audit + fix על commit c8a7f4b: skeleton loading קיים ותקין — CSS system, skeletonCard()/skeletonFeedCard() helpers, initDataFromFiles מציג skeletons ב-frame-1 לפני Promise.all. תיקון אחד נדרש: rgba(255,255,255,.06) ב-shimmer gradient → var(--skeleton-highlight) עם DS-004 fallback. נוסיפו --skeleton-base + --skeleton-highlight ל-:root (tokens מ-card/card-hover). shimmer direction נכון: card → card-hover → card. commit: bf6cddd. DoD: skeleton-card=16, shimmer keyframes=2, אפס standalone hex בCSS skeleton. חסם פתוח: Playwright test לא רץ (אין env). |
| 2026-06-19 | sam | feat/sqlite-likes | הושלם | Cycle 2 P1: SQLite persistence ל-post likes. הוחלף _likes_store: dict בטבלת post_likes ב-data/awear.db. init_db() נקרא ב-startup event — יוצר TABLE IF NOT EXISTS ומבטיח schema לפני כל request. _get_db() פותח connection עם row_factory + parameterized queries (? placeholders) — אין SQL injection surface. toggle_like: SELECT→DELETE/INSERT→COUNT בtransaction אחד. get_post: COUNT מDB בלבד. _likes_store לא קיים בקוד — אין fallback לdict. DoD: python3 import OK; schema test PASSED (insert+delete+count). commit: 0fccf74, worktree: /Users/tamargrosz/sam-sqlite-likes |
| 2026-06-19 | netta | feat/success-token-migration | הושלם | (1) hex_audit_cycle1.md תוקן: #1a1030=0, #2a2040=0 אחרי merge של fix/cycle-1-p0-visual (4cd0156). audit רץ על snapshot ישן — baseline היסטורי שמור, הערה נוספה. (2) #4ade80/#34d399 → var(--success) migration: 30 הופעות, DoD: grep=0. נוסף --success-dark:#22c55e לtokens.css. rgba(74,222,128) + rgba(52,211,153) עודכנו ל-rgba(82,201,122). #86efac נשאר בכוונה (tier ≥60 בscales). כל ההחלפות = success/earning context — אפס דקורטיביים שנדחו. |
| 2026-06-19 | mark | agents/plans/hex_audit_cycle1.md | החלטה | #fff migration — B (חלקי, context-driven). ניתוח 58 הופעות: ~50 הן color:#fff על accent gradient (CTAs/badges) — לא משתנות, pure white נכון לcontrast. ~5-8 הן background:#fff על image placeholders — לא משתנות, intentional. ~25-30 הן color:#fff על overlays/dark bg — ישתנו ל-var(--fg) fallback #fbfbfd. עדיפות: P2 (אחרי P0: #1a1030/#2a2040). timing: Cycle 3. חסם: screenshot comparison + גבאנה clearance לפני merge (DS-005). migration guide נכתב ב-hex_audit_cycle1.md. |
| 2026-06-19 | dana | feat/onboarding-screen | הושלם | OnboardingScreen stub: 3 שלבים (welcome/styleQuiz/camera), FlatList horizontal pager, dot indicator, CTA → navigate('CameraPermission') בשלב האחרון. כל copy דרך t() — namespace onboarding נוסף ל-en.json + he.json (8 keys כל אחד: ctaNext, ctaFinal, 3 * {title,body}). אפס emoji ב-UI chrome (DS-006) — icon placeholders = View עגולה, SVG Cycle 3. commit: 5ab9743, worktree: /Users/tamargrosz/dana-onboarding. חסמים פתוחים: (1) OnboardingScreen לא מחובר ל-App.js — ממתין לתיאום ארכיטקטורה עם רועי (MB-002, React Navigation Cycle 2); (2) Metro bundle EXIT 0 לא נבדק. |
| 2026-06-19 | dana | main | הושלם — 3 merges | מיזוג 3 branches לפי mobile_merge_checklist.md + הוראת וראן. שלב 1: feat/wardrobe-screen-stub → main (WardrobeScreen.js, App.js עם params + screenParams, wardrobe i18n 4 keys). שלב 2: fix/camera-compression-onpress → main (compressForUpload, capturedPrimaryButton onPress, expo-image-manipulator ב-package.json). שלב 3: feat/profile-screen-stub → main — conflict ב-en.json + he.json (wardrobe namespace מ-HEAD לעומת profile namespace מ-branch) — נפתר בשמירת שני namespaces. 3 worktrees הוסרו. mobile/screens/ כעת: 5 מסכים (CameraPermission, Camera, Feed, Profile, Wardrobe). חסמים פתוחים: (1) editProfile onPress ריק — ממתין לauth (אורן); (2) Metro bundle EXIT 0 — לא נבדק בסביבה זו. |
| 2026-06-19 | ayalon | agents/plans | הושלם | Marketplace Spec: כתב agents/plans/marketplace_spec.md. הכרעות: 3 tabs (גלול/שלי/רכישות) לפי status, sort default=Newest (per Cycle 2 roadmap). listing card לפי R-001: condition badge + size badge + seller handle + heart. Sell Form כ-bottom sheet: פריט מWardrobe + מחיר + condition + מידה + תיאור. תשלום + shipping + persistence = Cycle 3. DoD: 7 קריטריונים עם Playwright. הערת סתירה מזוהה: Marketplace לא שובץ ב-Cycle 2 roadmap — Tab Bar כולל tab ומוביל לריק. הצעה לBoard Sync: stub P1 Cycle 2, Sell Form P0 Cycle 3. |
| 2026-06-19 | varan | feat/wardrobe-screen-stub | מוזג | בדיקה לפני merge: WardrobeScreen.js קיים, route.params?.newImageUri מטופל, wardrobe namespace קיים ב-en.json+he.json (4 keys), כל copy דרך t(). MERGE: feat/wardrobe-screen-stub → main --no-ff. חסם פתוח: Metro bundle EXIT 0 לא נבדק (אין Bash בסביבת וראן) — דורש ריצה ידנית לפני prod. |
| 2026-06-19 | varan | fix/camera-compression-onpress | מוזג | בדיקה לפני merge: compressForUpload(uri) קיים (resize 1080px, JPEG 0.7, target <400KB), capturedPrimaryButton.onPress קיים ומנווט ל-Wardrobe עם newImageUri. MERGE: fix/camera-compression-onpress → main --no-ff. חסם פתוח: Metro bundle EXIT 0 לא נבדק — דורש ריצה ידנית לפני prod. |
| 2026-06-19 | varan | feat/profile-screen-stub | מוזג | בדיקה לפני merge: ProfileScreen.js קיים, MOCK_PROFILE מוגדר, stats row (posts/followers/following), CTA row (addLook→navigate('Camera'), editProfile placeholder), profile namespace ב-en.json+he.json (5 keys). MERGE: feat/profile-screen-stub → main --no-ff. חסמים פתוחים: (1) editProfile.onPress ריק — ממתין לauth; (2) Metro bundle EXIT 0 לא נבדק. |
| 2026-06-19 | roei | feat/react-navigation-install | הושלם | Cycle 2 P0: React Navigation install + Tab.Navigator. npx expo install: @react-navigation/native ^7.3.3 + @react-navigation/stack ^7.10.5 + @react-navigation/bottom-tabs ^7.18.2 + react-native-screens 4.25.2 + react-native-safe-area-context ~5.7.0 — כולם ב-package.json. App.js: NavigationContainer + createBottomTabNavigator + AppProvider. shim (SCREENS/buildNavigation/useState) הוסר לחלוטין. 3 Tab.Screens: Feed (FeedScreen), Camera (CameraScreen), Wardrobe (WardrobeScreen). Tab bar: #0e0c0f bg, #e8526a active, #8a8498 inactive. Icons ממתינים לmark — labels בלבד per plan. FeedScreen + WardrobeScreen + AppContext הועברו מworktrees קיימים. en.json + he.json: feed namespace (3 keys) + wardrobe namespace (4 keys). DoD: @react-navigation/native EXISTS + package.json מכיל 5 packages + 3 Tab.Screens + 0 t() fallbacks. חסם פתוח: CameraPermissionScreen לא ב-Tab — דורש Stack navigator בעתיד (Cycle 3). commit: 0e15041, worktree: /Users/tamargrosz/roei-react-nav |
| 2026-06-19 | sam | feat/auth-endpoints | הושלם | Cycle 2: auth endpoint stubs — POST /api/auth/register (username ≥3 chars, 409 duplicate), GET /api/auth/me/{user_id} (404 on miss), PATCH /api/auth/me/{user_id} (allowed: display_name/bio/avatar_url). in-memory _users_store; skeleton בלבד — אין passwords/tokens (Cycle 3). תיקון: dict|None → Optional[dict] לתאימות Python 3.9. DoD: python3 -c "import app" EXIT 0; grep auth endpoints = 3. commit: 0d8da55, worktree: /Users/tamargrosz/sam-auth |
| 2026-06-19 | dolce | main | הושלם | Cycle 2 P1 (Gabbana clearance): (1) מחיקת CAT_EMOJI object (~שורה 1607) — dead code, לא היה בשימוש; (2) מחיקת CAT_EMOJIS object מ-openSellForm — prefillEmoji הוסר, category select הוחלף ב-labels נקיים ללא emoji, שדה emoji הוסר מה-localStorage object; (3) pc-header: 👥 → icon('users',18) — header נמצא בJS template literal → icon() תקין (DS-008). אימות: grep CAT_EMOJI\|CAT_EMOJIS = 0. DS-008 scan: 0 emoji ב-static HTML headers. commit: b09b3bb |
| 2026-06-19 | steve | main | הושלם | Merge feat/cycle-1-backend (סאם): rate limiting + logging. ביקורת: (1) thread-safety תקין לdemo (asyncio single-threaded, documented). (2) middleware exception gap — call_next זורק → log לא נכתב. תוקן: try/finally מבטיח log גם כשיש exception. (3) endpoint names תואמים app.py. commits: 35836fd (fix) + merge. MG-005: הbug לא נתפס כי DoD של סאם = "python3 -c import app" בלבד — אין test על exception path. נדרש: middleware DoD חייב curl test + assert log אחרי exception מלאכותי. |
| 2026-06-19 | steve | main | הושלם | ANTHROPIC_API_KEY env setup: .env.example נוצר, .gitignore עודכן (!.env.example exception), python-dotenv + load_dotenv() אומתו כקיימים ב-app.py. api_key_alert.md עודכן עם resolution. commit: 159f38f |
| 2026-06-19 | steve | main | הושלם | Merge feat/data-api-endpoints (סאם) + feat/like-endpoint (שירה). ביקורת סאם: כל 4 נקודות תקינות — cache init ברמת מודול (list=[]), filter logic תקין, 404 תקין, אין startup כפילות. ביקורת שירה: תוקן None guard ב-toggle_like — request.client יכול להיות None מאחורי reverse proxy → crash AttributeError. תוקן: user_key = (request.client.host if request.client else None) or "anonymous". הוסר try/except NameError מיותר (_posts_cache מובטח). הוסר import כפול Request. commits: 807f287 (data-api) + b09b3bb (like+fix). worktrees הוסרו. MG-005: מה לא תפס? שירה לא כללה curl test ב-DoD לסצנריו proxy — request.client=None לא נבדק. נדרש: כל endpoint שמשתמש ב-request.client חייב DoD עם: curl --header "X-Forwarded-For: 1.2.3.4" + assert no AttributeError. |
| 2026-06-19 | dolce | feat/skeleton-loading | הושלם | Cycle 2 P0: skeleton loading — CSS system (.skeleton/.skeleton-card/.skeleton-text/.skeleton-avatar + @keyframes skeleton-shimmer, DS-004 fallback ב-:root), skeletonCard() + skeletonFeedCard() helpers (aria-hidden, 4/5 aspect-ratio, zero layout-shift), initDataFromFiles מציג 6 feed skeletons + 8 shop skeletons ב-frame-1 לפני Promise.all, renderFeed() ב-finally מחליף. בונוס P0: 🪄→icon('sparkle',52) ב-inspiration empty state; 🌀 הוסר מsf-empty; ✨→icon('sparkle',12) ב-sf-compat. grep skeleton-card=3. commit: c8a7f4b, branch: feat/skeleton-loading |
| 2026-06-19 | mark | main | הושלם | Design Approvals Cycle 2 — שלוש החלטות: (1) Mediterranean Modern palette APPROVED — WCAG AA/AAA עובר על כל הזוגות, drop-in replacement, tokens.css תקף. docs/COLOR_SYSTEM.md עודכן ל-APPROVED. (2) --t-body=14px (לא 15px) — AWEAR ישראלית-first, compact פרדיגמה, migration קל יותר. Token names: אפשרות A — tokens.css קיים, 10 גדלים סמנטיים, לא שינוי שמות (breaking change RN). DESIGN_STANDARDS יעודכן לתואם tokens.css. ערכי h1/h2/display: לא משנים לפני Cycle 2 migration + screenshots (DS-005). Action לנטה: הוסיפי --t-lg:19px לפני migration. (3) Tab Bar Icons — Feed:image, Camera:camera, Wardrobe:hanger, Marketplace:bag, Profile:user — כולם מ-ICONS הקיים, 22px, active:var(--accent), inactive:var(--muted). react_navigation_plan.md עודכן. tBody_alignment_question.md סגור. |
| 2026-06-19 | dana | feat/profile-screen-stub | הושלם | Cycle 2 P1: ProfileScreen.js stub — ScrollView, avatar (88px), displayName, username, bio, StatBox row (posts/followers/following), CTA row (Add Look → navigate('Camera'), Edit Profile placeholder). profile namespace נוסף ל-en.json + he.json (5 keys). כל copy דרך t() — אין hardcoded strings. colors מטוקנים קיימים (0e0c0f/e8526a/1e1a22) — migration ל-var() tracked ל-Cycle 3. commit: 9b24518. worktree: /Users/tamargrosz/dana-profile. חסמים פתוחים: (1) editProfile onPress ריק — ממתין לauth screens (אורן); (2) hardcoded colors — migration אחרי וראן מחליט על token naming. |
| 2026-06-19 | netta | main | הושלם | Cycle 2 DS: (1) CSS spinner class נוסף לsection Utilities בtokens.css — .spinner (20px), .spinner-sm (14px), .spinner-lg (32px), @keyframes spin. tokens בשימוש: var(--card) לborder, var(--accent) לborder-top-color. מוכן לדולצ'ה (P1-4 מגבאנה). (2) Hardcoded hex baseline audit Cycle 1: סה"כ 247 הופעות. top-3 P0: #fff (58), #4ade80 (22), #1a1030 (13). target cycle 2: ≤124. P0 migrations: #1a1030/#2a2040 (22 הופעות) → var(--bg)/var(--card). #fff (58) → var(--fg) — דורש screenshot comparison. קובץ: agents/plans/hex_audit_cycle1.md. |

## 2026-06-19

| תאריך | סוכן | branch | סטטוס | תיאור |
| 2026-06-19 | oren | feat/api-endpoints-integration | הושלם | Cycle 1 משימה שלישית: החלפת 3 קריאות fetch — profiles.json + posts.json + products.json → /api/profiles?limit=20 + /api/posts?limit=40 + /api/products?limit=100. schema update: data.items || data (fallback לarray ישיר לתאימות אחורה). DoD: grep -c "fetch('/static/data" = 0 עבר. commit: adf24a3. merge dependency: Sam's feat/data-api-endpoints חייב להיכנס לפני branch זה. |
| 2026-06-19 | netta | main | הושלם | Cycle 1 Task 3 — typography scale audit: cycle-opening grep = 0 שימושי var(--t-) בindex.html (DS-001 מאושר — tokens קיימים, לא בשימוש). DoD: --t-micro/h2/h1/display קיימים בtokens.css (4/4). לא שוניתי ערכים — המשימה ביקשה ערכים שונים (display:36px/h1:30px/h2:24px) מהקיים (32/24/18px), שינוי ללא review = DS-005 violation. נמצא פער קריטי: DESIGN_STANDARDS.md מגדיר token names שונים (--t-xs/sm/md/lg/xl) מtokens.css הקיים (--t-display/h1/h2/...) — דורש החלטת מארק לפני Cycle 2 migration. קובץ שאלות נוצר: agents/plans/tBody_alignment_question.md (3 שאלות: --t-body 14/15px, שמות tokens, ערכי h1/h2/display). migration לא מתחיל ללא תשובות. |
| 2026-06-19 | roei | feat/feed-screen-mobile | הושלם | Cycle 1 MB-003: FeedScreen.js — FlatList 3 hardcoded cards, getItemLayout (SLOT_HEIGHT=456), removeClippedSubviews:true, initialNumToRender:8, maxToRenderPerBatch:4, windowSize:5. feed namespace en.json+he.json (3 keys). interpolate() helper לcount. App.js מחובר. grep Hebrew=0, t() fallback=0. commit: 5b2077b |
| 2026-06-19 | roei | feat/feed-screen-mobile | הושלם | Cycle 1 משימה שנייה: AppContext.js (locale/capturedUri/feedPosts, useApp hook עם fail-fast throw). App.js עטוף ב-AppProvider. FeedScreen: RefreshControl (800ms simulate, tintColor #e8526a), useApp() מחובר, feedPosts fallback ל-SAMPLE_POSTS. locale default='he'. RTL+locale-switching: Cycle 3. API integration: Cycle 2. branch מוכן לmerge. commit: b33ae8c |
| 2026-06-19 | sam | feat/user-stats | הושלם — ממתין ל-Steve review | GET /api/users/{user_id}/stats — post_count + followers + following + total_likes. 404 guard על profile, user_posts filter מ-_posts_cache, followers/following מ-profiles.json (v1 static), total_likes מ-SQLite post_likes table (fallback ל-in-memory). python3 import OK. commit: 7e31acf, worktree: /Users/tamargrosz/sam-user-stats. session נחסם — session חדש אימת: worktree קיים + commit קיים + import OK. |
| 2026-06-19 | dana | feat/onboarding-screen | הושלם — commit 63d94a6 | OnboardingScreen — AsyncStorage persistence + i18n content. DoD עבר: AsyncStorage count=2, onboarding_complete count=1. בדיקה: AsyncStorage.setItem('onboarding_complete','true') + navigation.navigate('Feed') קיימים ב-handleFinish(). en.json: screen1/screen2/screen3 + skip/next/start — קיימים ומאומתים. he.json: screen1/screen2/screen3 + skip/next/start — נוספו (היו חסרים). 0 emoji, 0 hardcoded strings — כל copy דרך t(). worktree: /Users/tamargrosz/dana-onboarding, branch: feat/onboarding-screen. חסם פתוח: OnboardingScreen לא ב-Tab.Navigator (Stack navigator נדרש, תיאום עם רועי + וראן per MB-002). |
|-------|------|--------|-------|-------|
| 2026-06-19 | sam | feat/cycle-1-backend | הושלם | Cycle 1 Phase 4: rate limiting (analyze:5/min, outfit:10/min, chat:20/min) + structured request logging middleware ב-app.py. commit: 33b0465 |
| 2026-06-19 | oren | fix/look-total-usd | הושלם חלקית | BE-002: 2/3 מקומות תוקנו — שורות 2118, 2150 (look grid + shop-look button): ₪ → $. שורה 2305 (feed buy button) דחויה: post.price (ILS) \|\| look_total_usd (USD) fallback — דורש החלטת schema מסאם לפני שינוי סימן. commit: 7244a7b |
| 2026-06-19 | steve | feat/cycle-1-data-integration | approved + merged | Cycle 1: חיבור data files לUI — loadFeedData + loadProducts עם async/await תקין, fallback ל-SEED_POSTS/SHOP_SEED, schema mapping תואם renderFeed/renderShopGrid. נמזג ל-main. הערת חוב: renderShopGrid מציג ₪${it.price} אבל price=price_estimate_usd (דולר) — BE-002 חוב ידוע, cycle הבא. |
| 2026-06-19 | dana | fix/camera-compression-onpress | הושלם | Cycle 1 MB-004: expo-image-manipulator@^56.0.19 הותקן, compressForUpload(uri) נוסף (resize 1080px, JPEG 0.7, target <400KB), capturedPrimaryButton קיבל onPress עם compress→navigate('Wardrobe', { newImageUri }). commit: 00a8d05 |
| 2026-06-19 | ayalon | agents/plans | הושלם | SF-001: אישור moderation thresholds של שירה — MEDIUM = גלוי ל-poster בלבד (shadow-moderation, OR), HIGH = admin_log בלבד ללא push, reviewer = ג'ף דרך admin panel. readiness לפרודקשן תלויה ב-curl test + CI — עדיין פתוח לשירה. קובץ: agents/plans/moderation_thresholds_proposal.md |
| 2026-06-19 | ayalon | agents/plans | הושלם | Spec P1: skeleton loading לדולצ'ה — Shopping Feed ראשון, shimmer animation עם CSS tokens, הגדרת skeleton vs empty state, Playwright DoD. קובץ: agents/plans/skeleton_loading_spec.md |
| 2026-06-19 | ayalon | agents/plans | הושלם | Spec P1: style filter chips לדולצ'ה — 8 chips + הכל, OR multi-select, localStorage persistence, חיבור ל-activeFilter logic קיים, Playwright DoD. קובץ: agents/plans/style_chips_spec.md |
| 2026-06-19 | netta | main | הושלם | Cycle 1 DS-001: --text/#fbfbfd ו--bg2/#161318 נוספו כ-aliases ל-tokens.css (backward compat, 35 הופעות בindex.html). --t-title/--t-lead/--t-small נוספו ל-typography scale. awear-tokens.json מסונכרן. learnings.md DS-001 עודכן. migration מ-hardcoded font-size → Cycle 2. |
| 2026-06-19 | varan | main | הושלם | Cycle 2 תכנון: כתב agents/plans/mobile_cycle2_backlog.md (P0: React Navigation install + Tab Bar + WardrobeScreen stub; P1: FeedScreen→API + ProfileScreen stub; החלטות ארכיטקטורה nav+state מתועדות לפי MB-002) + mobile/navigation/AppNavigator.js skeleton (SCREENS object, הערות install). dispatch לא יצא עדיין — ממתין ל-Board Sync. |
| 2026-06-19 | dolce | fix/cycle-1-p0-visual | הושלם | Cycle 1 Phase 4 — תיקון כל P0 מביקורת גבאנה: (1) P0-1: 13 הופעות #2a2040/#1a1030 → var(--card)/var(--bg); (2) P0-2: 11 Quick Action buttons emoji → icon() — נוספו 9 icons חדשים ל-ICONS object; (3) P0-3: closet empty state → icon('hanger',60) + icon('camera',18); (4) P0-4: wardHits → productImage(); (5) P0-5: mySells שני locations → productImage(); (6) P0-6: sf-tabs → data-icon + JS injection. בונוס: analytics/seasonal/admin/stylist/compare/wishlist/OG headers ו-og-spinner emoji הוסרו. grep #2a2040=0, grep #1a1030=0. commit: 4cd0156 | branch: fix/cycle-1-p0-visual |
| 2026-06-19 | sam | feat/data-api-endpoints | הושלם | Cycle 1 משימה שנייה: 4 endpoints חדשים — GET /api/products (filter: category/color/in_stock + pagination), GET /api/posts (filter: user_id + pagination), GET /api/profiles (pagination), GET /api/profiles/{user_id} (404 on miss). startup cache: 3 JSON files נטענים פעם אחת ב-startup event (לא per-request disk I/O). נבדק: python3 -c "import app" EXIT 0; curl על כל endpoint מחזיר JSON תקין; filter category=shoes מחזיר 11 מוצרים; 404 על user_id לא קיים. commit: 8305516 |
| 2026-06-19 | shira | feat/like-endpoint | הושלם | Cycle 1 משימה שנייה: POST /api/posts/{id}/like (toggle, IP-based) + GET /api/posts/{id} ב-app.py. in-memory _likes_store. _posts_cache fallback (try/except NameError). curl-tested: like→{liked:true,likes:1} 200 OK; unlike→{liked:false,likes:0} 200 OK; GET empty cache→404 OK. python3 -c "import app" EXIT 0. commit: f829c78 |
| 2026-06-19 | oren | main | הושלם | Cycle 1 משימה שנייה: תיקון כפילויות avatar_url ב-profiles.json. נמצאו 4 כפילויות (לא 2 כפי שדווח): women/38 (user_008+017), women/82 (user_013+016), women/66 (user_003+018), women/84 (user_005+020). הוחלפו ה"מאוחרים": user_017→women/1, user_016→women/2, user_018→women/3, user_020→women/4. DoD: "OK — all 20 avatars unique" עבר. אין שינוי schema — לא נדרש אישור סאם. |
| 2026-06-19 | sam | fix/look-total-usd | החלטה schema | BE-002/BE-003: שורות 2395+2398 (feed buy button) — אפשרות B אושרה. post.price ב-SEED_POSTS הוא USD בפועל (149/219/89 וכו'), loadFeedData ממפה מ-likes (לא ILS מ-DB), אין מקרה שבו post.price = ILS. הוראה לאורן: (1) שורה 2395: `post.price\|\|post.look_total_usd\|\|0` → `post.look_total_usd\|\|0`; (2) שורה 2398: `₪${esc(post.price\|\|post.look_total_usd\|\|0)}` → `$${esc(post.look_total_usd\|\|0)}`; (3) שורה 2440: `price\|\|post.price\|\|post.look_total_usd\|\|total` → `price\|\|post.look_total_usd\|\|total`. |
| 2026-06-19 | dana | feat/wardrobe-screen-stub | הושלם | Cycle 1 משימה שנייה: WardrobeScreen.js stub — ScrollView עם newImageUri display (aspectRatio 4/5, borderRadius 12, newLabel בrose) + empty state (emptyTitle, emptyBody, CTA → navigate('Camera')). wardrobe namespace נוסף ל-en.json ו-he.json (4 keys: newItem, emptyTitle, emptyBody, cta). App.js: WARDROBE הוסף ל-SCREENS; navigate() שודרג לקבל params ולשמור ב-state; route.params מועבר ל-WardrobeScreen; goBack() map הורחב (Wardrobe→Camera, Camera→CameraPermission). Camera flow שלם קצה-לקצה: CameraPermission → Camera → compress → navigate('Wardrobe', {newImageUri}) → WardrobeScreen. commit: 1c97217 |
| 2026-06-19 | sam | agents/logs | P0 Alert | ANTHROPIC_API_KEY חסר מה-env. אומת: python3 → MISSING. /api/moderate fail-open — כל תוכן עובר. גילוי: שירה, Cycle 1 Phase 4. alert נוצר: agents/logs/api_key_alert.md. נדרש Steve/Jeff — set secret לפני deploy. |
| 2026-06-19 | ayalon | agents/plans | הושלם | Cycle 2 Product Roadmap: כתב agents/plans/cycle_2_product_roadmap.md. P0: skeleton loading (דולצ'ה), style filter chips (דולצ'ה), React Navigation + Tab Bar (רועי+דנה), Moderation live (סטיב+שירה — חסום על ANTHROPIC_API_KEY). P1: FeedScreen→API, WardrobeScreen stub, emoji cleanup, spinner, typography migration (20 שורות). שאלות פתוחות הוכרעו: Marketplace sort default=Newest (tabs לפי status), Profile MVP=avatar+username+bio+likes בלבד (sold/viewed=Cycle 3), Notifications=Cycle 3. Board Sync scope table: חסם מזוהה לכל סוכן + action item מפורש. critical path מתועד. |
| 2026-06-19 | varan | agents/plans | הושלם | Cycle 2 P0 — תכניות ארכיטקטורה mobile לפי MB-002 (nav+state לפני dispatch): (1) agents/plans/react_navigation_plan.md — install command לרועי, App.js עם NavigationContainer+Tab.Navigator (Feed/Camera/Wardrobe), 5 אייקונים מארק מספק (icon() מdoc/ICON_SYSTEM.md), color tokens קיימים, blockers לprod: ANTHROPIC_API_KEY (סטיב) + AppNavigator shim (רועי). (2) agents/plans/mobile_merge_checklist.md — checklist לfix/camera-compression-onpress (דנה) ול-feat/feed-screen-mobile (רועי), חסם Metro bundle EXIT 0 לשניהם, סדר מיזוג: wardrobe-stub → camera-compression → feed-screen. |
| 2026-06-19 | gabbana | main (commit 4110346) | BLOCKED | 3 P0 נסגרו (glow=rose, body=tokens, sf-card-img sans font-size:52px). 3 P0 חדשים: P0-X1=SEED_USERS avatar+cover emoji שורות 3444-3449+779; P0-X2=hardcoded #20202b/#191922/#1c1c26 שורות 163/358/417/1168 → var(--card)/var(--bg); P0-X3=typography migration לא ממוזג. Instagram Story Test: נכשל. |
| 2026-06-19 | mark | agents/plans/empty_states_design.md | הושלם | Empty States Design Guide — כיוון עיצובי לכל 5 מסכי ה-empty state. עיקרון: empty state ≠ skeleton (שגיאה שחייבת להיסגר). pattern אחיד: icon(48)+title(var(--t-h2))+body(var(--t-body))+CTA אופציונלי. טבלת מסכים: Feed/Wardrobe/Profile/Marketplace/Notifications — icon+copy+CTA לכל אחד. CSS מוגדר עם var() tokens בלבד, אפס hex, אפס hardcoded. "אסור בהחלט": emoji בhero, null/undefined גלויים, spinner. הוראות לדולצ'ה (i18n, DS-004 fallbacks, icon check ב-ICONS object). QA checklist לגבאנה (7 נקודות P0+P1 + שאלת העל). |
| 2026-06-19 | steve | main | הושלם | merge cycle — 3 branches נבדקו ומוזגו. (1) feat/api-endpoints-integration (אורן): commit 2e9f797 כבר על main מ-session קודם — branch absorbed, worktree לא קיים, לא נדרשת פעולה. (2) feat/success-token-migration (נטה): 30 removals (#4ade80/#34d399) + 29 additions (var(--success)) — verified. merge עם conflict resolution ידני: typography migration (HEAD) ו-success tokens (נטה) שינו אותם שורות — 18 conflicts נפתרו ב-python script (HEAD font-size tokens + THEIRS color tokens). commit: 3a32abf. worktree /Users/tamargrosz/netta-success-tokens הוסר. (3) fix/look-total-usd (אורן): post.price הוסר מ-fallback chain ב-3 מקומות (data-price, span display, openSheetLook call) — verified grep = 0 results. merge clean. commit: 3c80da4. worktree /Users/tamargrosz/oren-look-price הוסר. MG-005: 18 conflicts = signal שה-concurrent branches לא תואמות — צריך לפתוח OW-003 enforcement. |

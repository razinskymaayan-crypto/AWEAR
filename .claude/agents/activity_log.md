# agents/activity_log.md — יומן פעילות סוכנים

> **CANONICAL SOURCE** — הוסף כאן בלבד. Archive → `activity_log_archive.md`. Worktree copies = read-only snapshots, לא לכתיבה.
> פורמט: `| YYYY-MM-DD | agent | branch/file | status | short description |`

| 2026-07-05 | oren | auto/oren scripts/data_integrity.py + static/data/README.md | done | New data-integrity gate: 7 error checks (orphan items_tagged, unique ids, user_id refs, field+price validation, categories) exit 1; 3 warn checks (fragment drift, dup image_url, unreferenced count). Proved exit 1 on injected faults. README rewritten to real 200-product catalog. Logo-placeholder feed images -> IDEAS #45. Craft: oren IC. |
| 2026-07-04 | oren | main/static/data/posts.json + static/index.html | הושלם | Feed item-pill bug fix (founder-reported). ROOT: all 40 posts items_tagged used stale prod_NNN ids that never existed in products.json (real format prod_ss/jk/ct/...) -> 100% of pills opened broken sheet; SPA fallback name:prod.name||pid leaked raw id as garment name. FIX: (1) honest caption-matched remap of all 40 posts to real product ids (90s denim post->Trucker Jacket+leather ankle boots, etc), array-of-strings shape + lengths preserved, 76 tags 0 orphans. (2) hardened SPA fallback index.html ~4516: unresolved tag -> null + console.warn + .filter(Boolean) so raw id can never render as name (fail loud). (3) _posts_cache loads JSON @startup + --reload only watches .py -> restarted uvicorn so /api/posts serves fixed ids (web direct-load AND mobile /api/posts both fixed by one data change). DoD: grep prod_015/032/041=0; orphans via file AND live /api/posts=0; Playwright repro2.py all pills real names+img+price, 0 upload triggers; node --check PASS. UPLOAD SYMPTOM: NOT reproduced. Learning: BE-TAG-INTEGRITY (be.md). |
| 2026-06-29 | dolce | main/static/index.html | הושלם | Public profile -> real users (INBOX). renderUserProfile (~10160): (1) Rewrote BIOS u1/u2/u3 to real personas that read like the vibe (Tamar "looks like she didn't try · always does", Carmel "streetwear · loud sneakers · soft attitude", Maayan "fewer pieces · cleaner lines · more intent"); u4-u6 untouched. (2) LOOK_MANIFEST local const, PROFILE-GRID ONLY (global SEED_POSTS byte-identical, feed untouched): gridLooks = seeded posts FIRST (real items/captions) + deduped synthesized image-only tiles from real /static/img/users/<name>/lookN.jpg -- Tamar 3, Maayan 3, Carmel capped 12/17. Synth tiles {img,likes(static),items:[]} -- no fabricated items/prices (honesty). (3) Gabbana P0+P1: .up-thumb-likes (~CSS 469) -> rgba(0,0,0,.42) blur scrim pill #fff no text-shadow (legible over light+dark photos); gridLooks.length<4 -> "More looks coming" camera filler card (no void); image-only sheet -> "Shop this look -- coming soon" row not bare dead-end. Gabbana 9/10 PASS; check-render 0; Tamar+Carmel screenshots verified. shot.mjs gained user:<id> form. |
| 2026-06-29 | dolce | main/static/index.html | הושלם | Feed Stories row -> real users (INBOX #1). Replaced 6 fictional stories (Y2K Challenge/Noa/Yael/Minimal Day/Mika/Streetwear, gradient+initials) with the 3 REAL AWEAR users — Tamar(u1)/Carmel(u2)/Maayan(u3) — using real avatar photos (/static/img/users/<name>/avatar.jpg), kept "Add story" + first. STORIES array (~8898) carries avatar+userId+initials-fallback. renderStories (~9427): avatar items render <img loading=eager onerror=this.remove()> over a .story-initials fallback span -> never an empty circle; tap reads data-uid -> openUserProfile(uid) (real public profile) instead of toast. CSS fix: .story-inner position:relative + img position:absolute;inset:0;object-fit:cover so the photo fully overlays the centered initials (flex-sibling overlap bug caught in screenshot). Gabbana 8.5 PASS no P0; check-render OK; feed screenshot verified clean. |
| 2026-06-28 | dolce | main/static/index.html | הושלם | Home "Today's Look" carousel fix — killed the two-images-side-by-side hard 50/50 seam (looked like an unintentional collage on the launch screen). Rebuilt .ho-top into a single stylist-flatlay composition: primary garment fills card top as cover (.ho-hero-img inset:0, object-position:center 30%), second garment = small overlaid swatch bottom-right (.ho-swatch width:32%, aspect 4/5, --space-3 inset, --r-sm). o.bottom missing → cover-only, no empty box. Distinct classes (ho-hero-img/ho-swatch-img) keep productImage() intact. Swatch double-ring border (rgba(255,255,255,.7) inner + dark outer) reads in BOTH light & dark mode. Cleaned 3 stale var(--bg2) → var(--surface). Gabbana 8.5→9+ (P1 focal-overlap + P1 light-mode border + P2 explicit object-fit). check-render OK; DS self-check: emoji diff=0, t-sm/md/lg=0, hex -1. |
| 2026-06-28 | shira | main/static/index.html | הושלם | DM screen (user-to-user) built + wired to Sam's /api/dm/* backend. New view id="dm" + renderDM() router (dmPeer null=list, else=thread). List: avatar (img onerror=avatarFallback OR inline initials per SF-AVATAR-01), name, relative time (dmTimeAgo, number-driven), truncated last_message, unread badge. Thread: me/them bubbles (accent right / card left), optimistic append on send → POST /api/dm/send → refetch reconcile, Back btn → list. Rate-limit (429) + error toasts, demo-safe empty/error states (no crash). User msg text rendered with esc() (emoji allowed = user content); zero emoji in chrome. NAV RE-ROUTE: DM button data-view chat→dm. ABIGAIL MOVED: AI Stylist chat (section id=chat) now reached via "Chat with Abigail" btn in outfits view (og-abigail-btn → showView('chat')); chat header gained Back btn → showView('outfits'). /api/stylist/chat + stylistChat untouched. Added ICONS: arrowLeft, send, messageCircle (was silently falling back to tag). 45 dm-* CSS classes, all var() tokens w/ fallback, 44px+ touch targets, DS-009 ok. DoD: braces/parens/brackets/backticks balanced (python3), self-check emoji-in-chrome=0 in my ranges, bad-tokens=0. CURL (TestClient, SF-002): all 3 endpoints 200, validation 400/422, send id=14 returned. NOTE: no node in shell → verify-rendering (Playwright) PENDING. |
| 2026-06-28 | sam | main/app.py | הושלם | DM backend (user-to-user, NOT AI stylist) — new dm_messages SQLite table (owner_key/peer_id/direction/text/created_at/read) + index idx_dm_owner_peer_created in init_db(). 3 endpoints: GET /api/dm/conversations (sorted last_at desc, unread counts), GET /api/dm/thread/{user_id} (msgs asc, marks inbound read, peer meta from _profiles_cache), POST /api/dm/send (Pydantic DMSendRequest, validate non-empty + max 2000, rate 30/min). MG-005 on all, rate limits 60/120/30. Idempotent demo seed of 4 convos (user_001..004) per owner_key. Demo fallback (no 500) on DB failure — verified via patched _get_db returns 200. stylist_chat untouched. import app EXIT 0, TestClient all 3 endpoints 200 + validation 400s + read-mark 3→0 verified. grep: 3 endpoints + table + index present. |
| 2026-06-27 | orchestrator | main/static/index.html | הושלם | Closet Health Score — composite 0-100 metric at top of Analytics screen. Formula: utilization×0.4 + activeWear×0.3 + rewear×0.3. Grade A/B/C/D, adaptive color. Demo closet scores 98/A ("Your wardrobe is thriving."); new user starts ~35/C and climbs as they engage — demonstrates AWEAR value loop to investors. 13 CSS classes (.an-hs-*), all var() tokens, no bare hex, no emoji. check-render EXIT 0, Gabbana 8/10. Commit e0cdf12. IDEAS.md #4 shipped. |
| 2026-06-27 | orchestrator | main/static/index.html | הושלם | Analytics Dead Zone 'List in My Store' → prefilled item sheet (survey #1 commerce fix): openDeadZoneListSheet() shows each dead item with product image + name + ~$X earn (50% retail). Alert subtitle now shows forward payout ('earn ~$170') not just sunk cost. _deadZoneItems[] set each analytics render (TDZ-safe). Each item has 44px 'List' btn → openSellForm(item) prefilled. Fallback: empty state → marketplace sell tab. JS syntax OK, check-render EXIT 0. Commit 4ec8b68. |
| 2026-06-26 | orchestrator | main/static/index.html | הושלם | A6 demo reliability — avatar fallback: 4 external avatar imgs (feed card, 2x peopleCard, user profile) all sourced from randomuser.me had NO onerror handler — if that CDN is blocked/slow during the live investor demo, every influencer avatar becomes a broken-image glyph (violates "the image is the star"). Added avatarFallback(img) helper + .avatar-fallback CSS (initials circle on accent->accent2 gradient, mirrors approved .up-avatar-initials) + data-name on each img. Gabbana 8/10; applied both P1 fixes (parent-width fallback so width:100%% feed avatar never mis-sizes; 2px var(--line) border to unify with .up-avatar). All var() tokens, no hex, no emoji. JS syntax OK, check-render EXIT 0, jsdom DOM test PASS. |
| 2026-06-26 | orchestrator | main/static/index.html | הושלם | feat(demo): Built by Agents investor dashboard screen — new 'agents' view accessible from home quick-actions. Shows hero stats (18+ screens, 9 agents, 3wk), 2-col agent team grid (Jeff/Steve/Dolce/Sam/Valentino/Oren/Shira/Gabbana), live activity timeline (last 6 real commits), founding quote, and investor deck CTA. Directly supports investor pitch thesis: "$80K vs $2M". 57 CSS classes, all var() tokens, no bare hex, icon() everywhere, ag-invest-btn min-height:52px. JS syntax OK, check-render EXIT 0. Commit 68961d0. |
| 2026-06-26 | oren | main/static/index.html | הושלם | BE-002 closed (look_total_usd mixed fallback, open since cycle 1): seed posts showed "Look total $149" while their itemized rows summed to $547 — a contradiction at the demo's "Buy this look" moment. Fix = single source of truth: new lookTotalOf(post) sums priced items (fallback look_total_usd); feedCardHTML buy button + openSheetLook total + shopPostItems all derive from it. openSheetLook now computes total from the items it renders, fixing every caller. No CSS/token/emoji change. node --check OK, check-render EXIT 0, Gabbana 8/10. |
| 2026-06-26 | orchestrator | main/static/index.html | הושלם | L2 shoppable feed: added look_total_usd to all 27 SEED_POSTS (set equal to price field). Before: zero feed posts showed the "Shop $X" buy button (feedCardHTML guards on look_total_usd). After: all 27 influencer posts display a price badge and shopping CTA, making the For You feed fully shoppable for the investor demo. Single sed command, 0 logic changes, +27 look_total_usd fields (verified grep count 13→40). JS syntax OK, check-render EXIT 0. |
| 2026-06-25 | orchestrator | main/static/index.html | הושלם | A4a L1 Looks tab demo seed: _seedDemoCloset() now seeds FEED_KEY with 3 curated demo posts (Y2K Minimal/Urban Streetwear/Minimal Chic) each with a real outfit photo from /static/feed/ and items referencing demo closet. Before: Looks tab empty on first launch. After: 3-col Looks grid populated from first open. Guard: runs only when loadFeedPosts() is empty inside existing _DEMO_SEED_KEY idempotency check. JS syntax OK, check-render EXIT 0. Commit d7cfeb9. |
| 2026-06-25 | orchestrator | main/static/index.html | הושלם | A3/A6 demo-image fix: handleCheckout()+handleLookCheckout() now preserve image_url + brand_vibe(/brand) + id when a purchased item lands in the closet. Before, bought items dropped image_url -> _productImgUrl() fell back to random loremflickr photos, breaking L1 "clean catalog image" right after the buy->closet demo moment. Zero-regression (only copies fields when present). JS syntax OK, check-render EXIT 0. |
| 2026-06-25 | orchestrator | main/static/index.html | הושלם | L3 Resale Smart Sell Suggestions: forSaleHTML() now shows 5 least-worn items (≤7 wear_count) with 50% resale price, ssSellLabel() contextual wear reason, ss-card horizontal scroll row. data-ss-sell event listener calls openSellForm(it) directly. CSS: ss-wrap/ss-card/ss-list-btn/ss-card-img/ss-card-body (17 classes, all var() tokens, min-height:44px). JS syntax OK, check-render EXIT 0. Commit 8325f59. |
| 2026-06-25 | orchestrator | main/static/index.html | הושלם | A4b L4(a) Occasion-aware outfit picks on home screen: replaced generic "Look 1/2/3" with contextual occasion badges (Date Night/Work Ready/Street Style/Casual Day/Weekend Vibes/Editorial) + styling tip per outfit. Occasion selected by time-of-day + day-of-week; items ranked by style_tag overlap. CSS: .ho-occasion-badge (accent pill, 8pt grid) + .ho-tip (muted micro text). All tokens, no emoji, no bare hex. JS syntax OK, check-render EXIT 0. Commit c068189. |
| 2026-06-24 | orchestrator | main/static/index.html | הושלם | Demo closet seed (A4a L1 Polish): _seedDemoCloset() pre-populates wardrobe with 13 items (tops/bottoms/shoes/outerwear/bag/dress/jewelry) on first launch. All with real brand CDN image_url. Also seeds profile name "Noa" + 7-day streak (lastDate=yesterday) so home screen looks like an active user from first open. Guard: awear_demo_seeded_v1 localStorage flag. JS syntax OK, check-render EXIT 0. Commit 7cd44b1. |
| 2026-06-24 | orchestrator | main/static/index.html | הושלם | A6 demo reliability: scan always succeeds. Added _SCAN_DEMO_OUTFITS (3 realistic outfits with real retailer image_urls — Y2K Minimal / Minimal Chic / Urban Streetwear). Catch block in fileInput listener replaced from error-div to client-side demo fallback: JSON.parse(JSON.stringify(pick)) deep-copy, timestamp IDs, look_total_usd computed, mode='demo'. Outfit generator already had offline fallback (buildFallbackOutfits). JS syntax OK, check-render EXIT 0. |
| 2026-06-24 | orchestrator | main/app.py + static/index.html | הושלם | A7 backend complete: orders + credits SQLite tables added to init_db(). POST /api/orders (records purchase, 5% creator credit if influencer_id, MG-005, rate 20/min). GET /api/wallet (balance + history, MG-005, rate 30/min). Frontend: handleCheckout() + handleLookCheckout() fire-and-forget fetch('/api/orders') so purchases persist in SQLite. localStorage remains instant source of truth. Python syntax OK, JS syntax OK, check-render EXIT 0. Commit 98c9c34. |
| 2026-06-23 | orchestrator | main/static/tokens.css | הושלם | Light mode tokens: @media(prefers-color-scheme:light) block added. Warm off-white canvas (#FAF9F7), editorial-black accent (#14110F), hairline borders (#E9E5DF), lighter shadows. --hl:#E84A5F for brand highlights. Implements VISUAL_VISION.md v2 Founder override + MASTER_PLAN board decision #5 (light+dark auto). JS syntax OK, check-render EXIT 0. Commit 6aee20a. |
| 2026-06-23 | orchestrator | main/static/index.html | הושלם | Style filter chips complete — multi-select OR logic + localStorage persistence. Chips: All/Minimal/Streetwear/Vintage/Y2K/Luxury/Sporty/Boho/Dark Academia (removed Secondhand). activeFilter→activeFilters(Set). Both renderFeed instances updated. Commit 5d8fbe6. JS syntax OK, check-render EXIT 0. |
| 2026-06-23 | orchestrator | main/static/index.html | הושלם | Instagram-inspired feed + profile post redesign: (1) Feed layout — TikTok full-screen snap→Instagram scrollable cards. scroll-snap removed. feed-card-full: height:100%→auto, border-bottom 6px surface separator. (2) .fc-header (new) — circular avatar (initials/photo), @username + verified, relative time, Follow button, More button — all above image. (3) .fc-image-wrap (new) — position:relative, aspect-ratio:4/5 portrait container. fcbg+feed-img+heart-burst+item-pills+reactions+trend-badge all inside. (4) .fc-below (new) — action bar + like count + caption + tags + match below image. .fc-action-bar: absolute→static, horizontal row, icons only (no glass bubbles): ♥count | 🔖 | ↗ | [spacer] | 🛒$price | 🚩. .fc-likes: "X,XXX likes" bold. .fc-caption-blk: @username (bold) + caption text. (5) feedCardHTML() complete rewrite. (6) Like handler updated: also syncs .fc-likes div. (7) Filter chips: white backdrop→dark var(--surface). (8) Profile posts tab: 2-col cards→3-col square grid (.up-grid-ig/.up-grid-thumb, aspect-ratio:1). Tap thumbnail → sheet with same header+portrait image+items list. Profile and feed now use identical card design/layout. |
| 2026-06-23 | orchestrator | main/static/index.html | הושלם | User Profile 100-review fixes: (1) DS-004 — color:#fff→var(--text,#fbfbfd) in .up-avatar-initials + .up-follow-btn. (2) Post cards clickable — data-post-idx + addEventListener → sheet with look image + items list. (3) Store items clickable — data-store-idx + addEventListener → openUserWardrobeItem sheet with Buy/Wishlist CTA. (4) Product images — productImage() in wardrobe + store grids (was icon() only). (5) Compat score — hidden when wardrobe empty, shows "Add items" note instead of fake 70%. (6) Wishlist CTA added to detail sheet footer. (7) Stats: 4→3 (removed Posts, kept Followers/Following/Items). (8) Style tag pills in hero. (9) Block check at profile entry + More button → confirmBlockUser. (10) Listener leak fixed — el.innerHTML='' at top of renderUserProfile. (11) Follow button for posts without userId — condition !isMine&&(post.userId||post.user). (12) Follow toast personalized: 'Now following/Unfollowed {name}'. (13) ARIA: role=tablist/tab/tabpanel + aria-selected + aria-pressed. (14) Post cover height 110px→160px. (15) .up-store-item cursor:pointer + :active scale. (16) STORE_DEMO enriched with search_query for image generation. (17) .up-sheet-pi CSS for sheet product image. |
| 2026-06-22 | valentino | main/static/index.html | הושלם | Marketplace deep upgrade: (1) SEED enriched — MP_SEED 8→12 items + condGrade A/B/C + seller_rating + seller_sales + seller_badge + listed_days + occasion + material. MP_NEW_SEED 6→8 items + occasion + arrived_days. (2) State vars — mpSearchQuery, mpOccasionFilter, mpMaterialFilter, _mpPendingOccasion, _mpPendingMaterial (TDZ-safe). (3) Search bar — mp-search-bar always visible in Shop tab, live input filter by name/brand/seller/color, clear button. (4) Quick sort chips — mp-quick-sort horizontal scroll bar above grid: Best Match / Newest / Lowest Price / Trending. (5) Resale card upgrade — mp-seller-row (avatar initials circle + @handle + seller_badge Top Seller/Fast Shipper/New Seller), mp-seller-rating (star icon + rating + sales count), mp-cond-grade-row (A/B/C grade pill with color-mix colors + listed_days time ago), mp-buy-row with Buy Now + Make Offer CTA (44px). (6) Retail card upgrade — mp-size-badge on image (top-left, blur backdrop), isNewArrival badge (arrived_days<=7 → "New Arrival"). (7) Occasion filter in filter sheet (Shop + Community). Material filter in Community only. Pending/apply/reset all synced. (8) Chip-remove handles occasion/material. (9) star icon added to ICONS. (10) CSS: mp-search-bar/inner/input/clear, mp-quick-sort/mp-qs-chip, mp-seller-row/avatar/badge/rating, mp-cond-grade-row/mp-cond-grade/mp-listed-time, mp-buy-row/mp-offer-btn, mp-size-badge. DoD: JS syntax EXIT 0, python3 import EXIT 0, no emoji UI, no bare hex, all touch targets >=44px. grep suite: 12 counts verified. |
| 2026-06-22 | valentino | main/static/index.html | הושלם | Marketplace split Shop/Community: הסרת tab "All", שינוי "New"→"Shop" (icon shoppingBag, active-shop=accent3) ו-"Pre-loved"→"Community" (icon heart, active-community=accent2). mpCondFilter default='new'. Header דינמי: "Shop New Arrivals" vs "Community Closets". _buildFilterSheetContent() context-aware: Community=Condition first+ClosetMatch+Sort+Price+Brand+Color; Shop=Sort+Price+Color+Brand (ללא Condition/ClosetMatch). Empty state Community: "No pre-loved items yet. Be the first to sell!" + Start Selling CTA. CSS: active-shop/active-community aliases. JS braces 2124/2124. אין emoji keyboard, hex רק כ׺llback בvar(). DoD: mpCondFilter='new' default=OK, Community tab=OK, isCommunity if/else=OK. |
| 2026-06-22 | valentino | main/static/index.html | הושלם | Rich filter system: 8 filter state vars + 7 pending vars (TDZ-safe). Filter sheet with Sort/Price range/Color swatches/Condition/Brand/60%+ Match toggle. Active chips bar. Enhanced empty state. 20 new CSS classes. DoD: check-render pass, 0 bare hex. |
| 2026-06-22 | orchestrator+valentino+sam | static/index.html + app.py + tokens.css | הושלם | Expanded Seasonal Summary (11-section editorial scroll): Color Story (Pantone swatches, 5 dominant colors), Active Ratio (% worn, 3 tier labels), Category Utilization (6-cat bars), Brand Universe (top 5), CPW by Category. 4 new data viz tokens. DS-010: hex swatches = user data = inline style exception. |
| 2026-06-22 | sam | app.py | הושלם | Analytics season API: _get_current_season() + _season_date_range() + _season_display_name() helpers. GET /api/analytics/wrapped/{year}?season=summer\|winter. GET /api/analytics/season/current. GET /api/analytics/seasons/archive. All MG-005, rate limit 60/30/min, demo fallback. DoD: grep >8, python3 import EXIT 0. |
| 2026-06-22 | valentino | static/index.html | הושלם | Analytics Wrapped teaser→Season Style Report: getCurrentSeason()/getSeasonStats()/renderSeasonTeaser()/openSeasonReport() (3814-3924 lines). 12 new CSS classes, 3 live stats card + "See Full Report" sheet. No emoji, 44px targets. DoD: grep >75 hits. |
| 2026-06-23 | orchestrator | main/static/index.html | הושלם | A4b Outfit Generator visual upgrade: renderOutfitResults() text→2×2 image grid. CSS rewrite: og-items-grid/og-item-cell/og-cell-label/og-shop-badge/og-closet-badge/og-wear-btn (11 classes, all var() tokens). Each outfit: 2×2 aspect-ratio 3/4 product images. Resolved Hebrew merge conflict. JS syntax OK, check-render ✓. |
| 2026-06-19 | dolce | main | הושלם | Gabbana audit P1-A+P1-B: Removed 🎉 from 2 toast strings. compat badge: onclick→data-action=compat-overlay. 3 onclick purchase-modal (lines 2560/4063/4384) = static HTML, out of scope. DoD: 🎉=0, onclick openCompatOverlay=0, data-action refs=4. |
| 2026-06-19 | netta | main | הושלם | CSS fixes P0-B/C: mp-compat-btn duplicate removed. P0-A: 28x28→44x44 touch targets. P1-C: dead CSS (mp-tabs/mp-tab) removed. P1-D: contrast 4.05:1→18.8:1. All DoD pass. |
| 2026-06-19 | dolce | main | הושלם | Marketplace Features 1+2: AI Filter Assistant (mp-assist-bar + runMPAssist) + Closet Match Overlay (openCompatOverlay). 11 new CSS classes. DoD: 16 grep hits for filter assistant, 0 for second query. |
| 2026-06-19 | netta | main | הושלם | CSS mp-assist-bar (Filter Assistant) + mp-compat-btn (Compat Badge). 10 grep hits, hex count 89. |
| 2026-06-19 | sam | main | הושלם | POST /api/marketplace/assist endpoint. MG-005, 20/min rate limit, Claude haiku-4-5-20251001, max_tokens=800, demo fallback. DoD: grep 6, curl 200, 429 at request 21. |
| 2026-06-19 | dolce | main | הושלם | Marketplace Round 2 (GOAT model): 3→2 tabs (shop/sell). mpCondFilter='all'. loadMPListings() includes MP_NEW_SEED. renderMPShopGrid replaces old grids. handleMPBuy() wired. Condition chips (All/New/Pre-loved). DoD: 19 grep hits for new identifiers, 0 for old ones. |
| 2026-06-19 | dolce | main | הושלם | Marketplace split: 3→2 tabs (secondhand/new/sell). MP_NEW_SEED (6 items) added. renderMarketplace rewritten with mp-segment. Default mpTab='secondhand'. DoD: old identifiers=0. |
| 2026-06-19 | steve | main | הושלם | Merged 15 branches (cycle 2 cleanup). Conflicts resolved: profile-real-data, api-improvements (MG-005+notifications preserved), auth-endpoints (users table added), mobile-tokens, wardrobe-screen, edit-profile, onboarding, register, rn-token-adoption, feed-api, visual-redesign (40 conflicts), skeleton-loading, notifications-ui, comments-ui, marketplace. python3 import EXIT 0, JS syntax OK. |
| 2026-06-19 | netta | feat/rn-token-adoption | הושלם | FeedScreen.js RN token adoption: hex→colors, fontSize→typography, padding→spacing, borderRadius→radii. 18 token references, hardcoded hex functional=0. commit 7a9793b. |
| 2026-06-19 | dana | fix/camera-compression-onpress | הושלם | Cycle 1 MB-004: expo-image-manipulator@^56.0.19 installed. compressForUpload(uri) added (resize 1080px, JPEG 0.7, <400KB). Commit 00a8d05. |
| 2026-06-26 | autopilot | main / static/index.html | הושלם | INBOX: removed weather feature from home screen (HTML card + CSS .home-weather-card/.hw-* + fetchWeather JS). Tightened greeting bottom padding 12→8px, aligned scan-prompt margin to 18px. Gabbana 8.5/10. Also fixed shot.mjs to seed awear_onboarded so screenshots show real screens. check-render OK. |
| 2026-06-26 | autopilot | main (report only) | הושלם | INBOX: Store walkthrough for founder. Screenshotted marketplace (shot.mjs) + sent Telegram screenshot + full feature guide (Shop/Community/My Store tabs, search, AI Stylist bar, category filters w/counts, Filter & Sort sheet, Matches My Closet, card badges/compat%/CO2, seller storefront). No code change. Note: tg.sh (curl) exits 3 in sandbox - used node https sender. |
| 2026-06-27 | valentino | main / static/index.html | הושלם | My Store polish (Gabbana feedback): ms-list-btn color:#fff→var(--on-accent,#fff); Live badge font-size:9px→.ms-live-badge class w/ --t-micro; stat strip — Profit emphasis (.ms-sx-profit success-tinted+15px), labels shortened (Listed/Sold/Avg/Profit/Today) no-wrap; ms-act/list-btn vertical padding unified to --space-3 (12px), primary/secondary via bg+weight only; Views btn refresh icon(9) affordance; ms-in-bar-label width→min-width 74px. DoD grep: t-sm/md/lg=0, ms color:#fff=0, live 9px gone, braces/parens balanced. Note: no node in shell → verify-rendering/Playwright PENDING. |
| 2026-06-27 | valentino | main / static/index.html | הושלם | INBOX survey: 100-expert panel on Closet Analytics (≈6.4/10). Shipped 5 fixes in renderAnalytics+CSS: (1) .an-sec-title var(--t-small,13px) DS-004; (2) Health+Rewear rings → true conic-gradient progress arcs w/ ::before hole; (3) "Utilization · worn 1+×" + rewear "2+ times — true favorites" disambiguation; (4) actionable weakest-lever Health hint ("Wear N never-worn items to reach Grade X"); (5) Hidden Cost card tappable→openSellForm + "Recover ~$X" CTA + :active feedback. Gabbana 8.5/10. check-render OK. |
| 2026-06-27 | valentino | main / static/index.html | הושלם | INBOX: redesigned My Store "Insight" sheet from a duplicate of the top stats strip into an actionable advisor. Rewrote _buildStoreInsight()+_renderStoreInsight(): Store Health score (0-100 heuristic + verdict + 3-KPI snapshot Revenue/Conversion/Saves, distinct from strip Listed/Sold/Avg/Profit/Views); "Do next" self-hiding recommendation cards (stale->real Refresh relist, incomplete-listings, pricing outliers, list-next->openSellForm) tier colors warn/priority/suggest (no --danger); Views->Saves->Sales conversion funnel + diagnostic; weekly momentum goal. Deleted old Performance/Audience/By-category/Top-performers blocks. New .ms-in-health/-snap/-rec/-goal CSS, border-inline-start RTL-safe, bar transition, min-width:36px. Gabbana 8.5->polish->9.5. check-render OK. |
| 2026-06-27 | shira | main / static/index.html | done | Feed social proof seeded (longest-idle agent, via ayalon). Posts showed 2.4k-7.2k likes but 0 reactions + "No comments yet" = reads fake. Added FEED_COMMENTS_SEED (80 unique look-specific comments, 3-9/post scaled to likes, each post = shopping-intent Q + creator reply = ask->answer loop) + FEED_REACTIONS_SEED (~10% of likes, uneven split, irregular numbers). seedFeedSocialProof() idempotent->localStorage (fixes toggle count-drop); openCommentsSheet empty-API no longer wipes seed. Sheet redesign Gabbana 7->8.5: per-comment avatar (inline initials span - fixed onerror-never-fires-on-valid-gif bug), creator-reply highlight + Creator badge, username --accent->--fg @ --t-caption, flag 44x44. Emoji only in comment-text data (DS-008 ok). check-render EXIT 0. Post-ship survey 8.3/10 (no severe). commits adbeac7+bc89e4f. |
| 2026-06-28 | valentino | main / static/index.html | הושלם | Rebuilt Community tab (Store/preloved) from scratch — Browse & My store untouched. New: renderMPCommunity() gated on mpTab==='shop'&&mpCondFilter==='preloved'; horizontal store strip (Following→Explore groups, 6 chips w/ avatar+follow pip); sub-toggle Store pages⟷View by items (mpCommunityView state); Mode A = store-page cards (following-first) + drill-into single store page (3-thumb preview, Visit/Back, mpStore state); Mode B = reuses renderMPShopGrid w/ existing mp* filters+toolbar+chips. Extracted shared card markup into renderMPCards() (Browse+store-page reuse, no dupe filter system). New state vars mpCommunityView/mpStore declared @6077 (TDZ-safe, before renderMarketplace). Tab-reset handler resets community sub-state. New CSS .mp-store-strip/.mp-store-chip*/.mp-comm-toggle/.mp-comm-tgl/.mp-store-pages/.mp-sp-* @~1579 (all var(--token,fallback), follow uses --on-accent,#fff). DoD: emoji UI chrome in new ranges=0, t-sm/md/lg=0, all hex are token fallbacks. Verify: check-render.mjs EXIT 0 (no init/TDZ errors) + JSDOM interaction test PASS (Community tab→2 strips/6 chips, toggle→items grid 12, drill store→detail+back+seller grid, follow toggle, Browse no-strip, return defaults to Store pages); only console noise = offline-fetch + missing tokens.css link (harness artifacts, not my code). No commit — awaiting founder review. |
| 2026-06-28 | shira | main / static/index.html | done | DM audit fixes (Gabbana 7.5/10). P0 contrast: .dm-bubble-wrap.me .dm-bubble + .dm-badge + .dm-send color var(--fg)→var(--on-accent,#fff) (AA ~2.0→pass on accent gradient, matches .ms-list-btn pattern); badge font-weight 800→900. P1 a11y: .dm-row role=button tabindex=0 aria-label="name, N unread" + keydown Enter/Space shares open() handler + :focus-visible accent outline. P1 read-state/grouping: dmBubbleHTML(m,prev,isLast) — timestamp only on side-change or >5min gap (DM_GROUP_MS via dmSameSide/dmCloseInTime), "Sent" status on last me-bubble (.dm-bubble-status), .grouped margin-top:-6px tightens 8px thread gap→2px same-side; optimistic send append derives prev from last bubble + strips stale Sent. P2 RTL: border-bottom-*-radius→border-end-end/start-radius (tail flips in RTL). P2 i18n: NO dm namespace in en/he.json + dmTimeAgo units would need HE plurals = net-new infra, logged as known debt (not built per audit "if cheap"). DoD: grep verified all 6; t-sm/md/lg=0; emoji-in-chrome in DM region=0 (28 file-wide all pre-existing data, unchanged vs HEAD); braces/parens/backticks balanced; 3 DM #fff all var(--on-accent,#fff) DS-004. No node → verify-rendering/Playwright PENDING. |
| 2026-06-28 | oren | main / app.py | done | Integration hardening (idle-agent task, via steve): POST /api/dm/send accepted ANY to_user_id -> silently persisted orphaned ghost threads against non-existent peers (Community u1-style ids vs DM user_NNN namespace). Added recipient-existence guard before INSERT (if _profiles_cache and not any(p.get('id')==to_user_id ...) -> 404 unknown recipient); fail-OPEN on empty cache (mirrors follows-toggle guard ~line 1408) so a transient cache miss never blocks every legit send. MG-005 + rate-limit untouched, no rename (OW-001 N/A), BE-005 SQLite path preserved. Verified via FastAPI TestClient (startup-triggered, cache=20): user_001->200, u1->404, empty->400; empty-cache user_001->200 (fail-open). AST OK, check-render OK. |
| 2026-06-28 | netta | main / static/index.html | done | DS-004 image-placeholder tokenization (longest-idle agent, via mark): 8 near-white hardcoded backgrounds glared bright-white in dark mode behind product images. Tokenized .item-hero/.combo-thumb/.sheet-look-emoji containers + their .img-fallback (#fbfbfd->var(--card), #b8b8c4->var(--muted)); shimmer skeleton ->var(--card)/var(--card-hover); .sf-badge.missing ->var(--warning)/var(--bg); inline sell-reason color ->var(--muted). Then Gabbana P1-A: .item-hero img + .combo-thumb img background:#fff->transparent so letterbox bars show container var(--card) (white in light=catalog look, dark in dark=no glare). Catalog <img> pixels untouched. grep: 6x var(--card,#fbfbfd), 3x var(--muted,#b8b8c4), 0 bare #fbfbfd. Gabbana 9/10, check-render EXIT 0. |
| 2026-06-28 | sam | main / app.py | done | Order-flow idempotency + validation (idle-agent task, scoped by steve). POST /api/orders was non-idempotent — a double-tapped/retried Buy (client fire-and-forgets from 2 call-sites with .catch) created duplicate orders AND double-credited the creator, violating MASTER_PLAN locked decision #11 (credits ledger append-only, idempotent). Added optional client_ref idempotency key to OrderCreate + orders table (additive: client_ref TEXT DEFAULT '' in CREATE TABLE for fresh DBs + guarded PRAGMA table_info ALTER for existing DBs + non-unique idx_orders_userkey_ref; NO UNIQUE constraint, no DROP/recreate, credits schema untouched). create_order dedups when client_ref non-empty (SELECT-before-INSERT, returns existing order_id + deduped:true, credit_amount 0 — no double-credit); empty client_ref = unchanged legacy behavior (backward-compatible). Added amount_usd<0 -> 400 + round(amount_usd,2) (0 allowed for unpriced looks). Lifted magic 0.05 -> named CREATOR_CREDIT_PCT (value unchanged, locked). Verified via FastAPI TestClient on throwaway DB: dup -> same order_id+deduped:true, orders=1/credits=1; negative -> 400; no-ref -> 200 credit 2.5; old-DB migration preserves rows. AST OK, check-render EXIT 0. Backend-only. Frontend handoff (stable client_ref per checkout) logged to IDEAS.md. Reverted test inserts to data/awear.db. |
| 2026-06-28 | dolce | main / static/index.html | done | WOW item-sheet closet-match band (founder INBOX #1, chunk 1/3, via mark). Replaced weak matchPercent tiny badge in openSheetItem with a prominent hero BAND driven by the richer calcCompatScore {pct,matches}: big % number (clamp 38-46px, weight 600) as the typographic anchor, 'match to your closet' caption, plain-English reason ('Pairs with N pieces you already own' / names 1-2), and 2-3 named closet chips. Tier colors (>=80 success / 60-79 accent2 / <60 accent) on the number + chip borders ONLY, band bg stays --surface. Empty closet -> inviting 'Add clothes to see your match' + Build-your-closet CTA (no dead 0%). New matchBandHTML() + .match-band* CSS, all var(token,fallback); removed .item-match rules. Gabbana 8.5/10 PASS P0-clean; applied 3 polish P2s (bigger hero number, muted lighter % glyph, reason spacing). check-render exit 0; sheet screenshot verified (98% band, 3 chips, buy button). matchPercent left intact (other callers still use it). |
| 2026-06-28 | valentino | main / static/index.html | done | WOW item-sheet "Where it sells" (founder INBOX #1, chunk 3/3, via mark). openSheetItem now adds a distinct buy-to-source block AFTER the stylist combos: editorial header (icon('tag') + "Where it sells"), a quiet "From $X" price headline (.wis-from, --t-lead), then the real retailers via the EXISTING storeRowHTML over itemStores(it), plus ONE subtle resale signal row (Depop, scope "Resale from $Y", Y=round(price*0.5)=canonical RESALE_SUGGESTION_PCT app.py:242). Depop filtered out of retail rows so it appears once. Footer "Buy at <retailer>" untouched. Gabbana 7->8.5 PASS after 3 P1 fixes: retail rows get scope:o.scope||"In stock" so every row is two-line (resale no longer the only subtitled/dominant row -> mark's "subtle not banner"); .wis-from color var(--text,#14110f) (DS-004); price headline guarded by basePrice (no "From $0"); +P2 Depop logo "DEP", section margin-top var(--space-6). All var(token,fallback), no emoji (icon('tag')). check-render EXIT 0; sheet screenshot verified (From $140; ZARA/ASOS/Mango two-line; Depop "Resale from $70"; Buy at ZARA $140). New shot_sheet.mjs added to capture the triggered bottom sheet. |
| 2026-06-29 | sam | main / app.py + scripts/scan_smoke.py | done | Real-scan liveness probe + SDK-shape hardening (INBOX, via steve; oren security PASS). Builds on 06-28 diagnostics. (a) GET /api/scan-health?probe=1 now makes ONE minimal real Claude call (messages.create, haiku, max_tokens=5) to prove the key is actually VALID, not just present -> bounded key_valid + probe_error(none|no_api_key|auth|rate_limit|timeout|sdk_shape|unknown); default no-probe path unchanged (no call, key_valid:null), separate 3/min bucket via check_rate_limit_window so the paid path can't be hammered. (b) /api/analyze hardened: wraps client.messages.parse and on AttributeError retries client.beta.messages.parse(betas=[structured-outputs-2025-11-13]) so an older deploy-box SDK (parse beta-only) runs LIVE instead of silently dropping to demo. (c) Factored exception->enum into _classify_api_error (new sdk_shape bucket). (d) scan_smoke.py now also prints key_valid via ?probe=1. Security: no key material / no str(e) in any response (oren A-D PASS). OW-001 grep: zero FE/mobile consumers of scan-health/key_valid/probe_error (additive). py_compile OK, check-render green. Cannot run LIVE from keyless CI box -> human one-command confirm in TODO_FOR_TAMAR. |
| 2026-06-29 | dolce | main/static/index.html | הושלם | Profile (closet view) editorial polish — INBOX "Zara×Vogue minimal-premium, Gabbana 8.5+", via mark. Mark scoped a 5-item punch-list; dolce implemented: (1) loud amber "Summer 2026" season card → quiet var(--card) row w/ 1px var(--line) hairline, sun+arrow var(--accent2), s-summer recolor block deleted (identical every season); (2) "Share look to feed" de-glowed — removed off-token purple box-shadow, demoted filled gradient → outline (transparent + line border + accent arrow), filled "Buy this look" primary untouched; (3) product-name 2-line clamp (no more "White Ribbed Cr...") @ --t-small; (4) shelf-title → --t-h3 -.2px, leading icon shrunk 18→15 + recessed --accent2, count → --muted --t-caption; (5) quieter cards — single hairline, .pimg 88→104 + tile 122→132. Gabbana 7.5 → applied 5 re-gate fixes: P0 letterboxing killed (.pimg img object-fit contain→cover + overflow:hidden plate = full-bleed catalog photos), white-glow CTA shadow → dark lift, seg-control overlap (margin-top -8px→space-3), per-tile "Shop" demoted filled-black → quiet secondary outline (min-height 44px), .pacts margin-top:auto baseline-align. Gabbana 8.5/10 PASS no P0 ("release to founders"); box-in-box → IDEAS #28. check-render EXIT 0; closet screenshot verified full-bleed (real headless Chromium). DoD: bad-tokens=0, hex 674→670, braces/parens balanced. |
| 2026-06-29 | dolce | main/static/index.html | done | Feed (default landing) editorial pass — INBOX "Zara×Vogue minimal-premium, Gabbana 8.5+", via mark. Gabbana audited current Feed at 6/10; dolce implemented all P0+P1+P2-1/2-2: (P0-1) loud pink filled "Your look" pill -> quiet transparent var(--muted) status label, weight 700, no backdrop-filter; (P0-2) three dark item-pills FLOATING over the hero photo -> moved to a quiet horizontal strip BELOW the image (.fc-below), 32px transparent + 1px var(--line), icons --muted, click data-attrs preserved (mobile-safe, no hover-reveal per mark); (P0-3) "95"+flame trend-badge REMOVED from hero (gamification noise, not editorial); (P1-1) active "All" chip filled-black -> hairline outline matching tabs; (P1-2) .feed-filters bg --surface->transparent + dropped double border (unified header); (P1-3) action bar weight 800-900 -> 600-700, buy-btn --accent3+900 -> --fg+700, like/save accent ONLY on .liked/.saved (one-accent discipline, NOT grayscale); (P1-4) username 13/900 -> --t-h3(15)/700, time 500, likes/caption 700; (P2-1) 6px separator -> 1px var(--line); (P2-2) .is-mine accent border-left removed. Gabbana re-gate found 1 blocker (.fc-username-tap font-weight:900 overriding 700 for non-mine posts) + 1 P1 (32px pill hit-area) -> dolce fixed: font-weight:inherit + .fc-pill::before inset:-6px = 44px touch target. Gabbana PASS 8.5/10. check-render exit 0; feed screenshot verified (clean hero, no floating chrome). All var(token,fallback), no emoji. |
| 2026-06-29 | dolce | main/static/index.html | done | Item-detail sheet editorial pass — INBOX 'Zara×Vogue minimal-premium, Gabbana 8.5+', via mark (last core screen after Feed/Profile). Gabbana 6/10; mark scoped 5 batches; dolce implemented A-E: killed .sheet-buy pink-glow box-shadow rgba(255,61,119,.42)+900->700+#fff->var(--on-accent); removed .item-price/.sheet-total-amt accent gradient-clip rainbow->solid var(--fg) 700 (Poppins+--t-h1 kept); weight ceiling 700 (.item-name/.stylist-h2/.combo-tag/.combo-cap/.wis-from, .item-brand->600); off-token .combo/.sheet-look-row->var(--card)/var(--line), .combo-tag bg->var(--accent), .combo-nm->var(--fg), .sheet-footer border->var(--line); fixed light-mode bug var(--text,#14110f) near-black-on-dark->var(--fg,#f0ecf5) on .wis-from/.combo-cap; .item-hero shadow .4->.18, .combo-cap .dot gradient->solid accent2, .combo-empty purple->card/line dashed; .item-hero-cat 800->700, .combo-plus 800->600. DEFERRED .sheet-look-emoji (look sheet, real img). Gabbana re-gate 8.5/10 PASS. DoD grep: targeted rgbas in 674-838=0, #14110f=0, bad t-tokens=0. check-render exit 0; before/after screenshots verified (CTA glow gone). Confirmation survey (ayalon ~100 synth) 6.5->8.06 no SEVERE. commits fa1df6c+b88c5fc. |
| 2026-06-30 | valentino | main/static/index.html | done | AI Stylist "Today's Look" daily contextual hero — pinned card at top of #outfits, headline of the AI tab. Adapts to REAL day-of-week + hour: shared occasion engine (pickOccasionForNow) extracted from home so both share one source of truth. Hero = live eyebrow ("Tuesday Late Night · Casual Day") + look name + outfit image from user's OWN closet (buildFallbackOutfits) + paired swatch + items + ONE quiet CTA "See the full look ->". Graceful empty state. Two secondary entries (Chat with Abigail / Style Swipe). New CSS .og-daily/.ogd-*/.og-entries (light-theme var() fallbacks, 8pt rhythm, >=44px). Gabbana 7.5->9/10 PASS. check-render EXIT 0, screenshot + 1-item edge verified. Phase-2 LOOK-swiping deferred (IDEAS #36); premium quota 3 free/day (IDEAS #35). |
| 2026-06-30 | oren | main / scripts/detect_ci_failure.sh | done | SELF-HEAL fix (CI self-healing diagnostic, via steve). Root cause: detect_ci_failure.sh recorded the WHOLE job-log tail -40 -> always post-job cleanup noise (git config, orphan-process cleanup, Node-20 deprecation), NEVER the failed step's real output, so every [UNRESOLVED] entry was undiagnosable (today's run 28462417666 = exactly this). Fix: PRIMARY path now downloads the per-step run-logs zip (GET /actions/runs/{id}/logs), case-insensitive substring-matches the failed $STEP filename (highest <n>_ prefix wins on collision), noise-filters, tail -60; FALLBACK to the old whole-job-log tail -40 if zip/unzip/match fails (never regresses); tags source: step-log|job-tail-fallback; adds likely_cause: from failed-job duration (>=29m -> 'timeout-minutes:30 likely hit', else 'transient API/OAuth/rate-limit'). Non-fatal throughout (set -u, all curl/unzip/python guarded, temp cleanup). Lead review caught + fixed the noise regex (was 'Node.js 1[26]' which never matched the real 'Node 20 is being deprecated' line -> now 'Node(.js)? [0-9]+ .*deprecat'). App verified healthy (check-render EXIT 0); run 28462417666 re-marked [FIXED] (real cause unrecoverable from CI box; not an app bug). autopilot.yml + timeout-minutes deliberately untouched per steve. |
| 2026-06-30 | jeff | main / static/index.html + app.py + knowledge/* | done | Carmel/Razi UX product-fix pass (24-item backlog, plan: snappy-percolating-pascal). Orchestrated as parallel agents-in-worktrees + Jeff-merge. SHIPPED & PUSHED (origin/main 91cbd67): Phase1 (11 quick wins, fdd9826) + Valentino item-sheet (product-page/match-ring/iOS dismiss, 167facb) + Sam daily_logs+streak (b217111) & orders/stories endpoints (3e61da1) + Shira feed-comments (859ed15) + Dolce +menu/daily-doc (5459896) + Valentino Community-filter/flat-lay-collage (9584e71) + Dolce DM-polish (888609c). 6 worktree merges, ZERO conflicts (CSS added near distinct anchors per OW-010). Every commit: node --check + Playwright (0 runtime errors). LEARNINGS CAPTURED (Carmel priority "agents must learn for next time"): OW-007 (live-data mapping hardcoded category:'top' → all pills shirt; resolve real field), OW-008 (.fca-ico vs .fca-icon → double-tap crash; grep class from render site), OW-009 (orphaned comment sheet found+wired not duplicated; grep before build), OW-010 (parallel agents on monolith = worktrees + distinct CSS anchors + serial merge), DS-018 (mobile bottom-sheet drag-to-dismiss on grab-strip only, not scroll-trap). Reinforced OW-001/BE-IDEMPOTENT (Sam extended live orders table additively vs redefine, 2 FE callers preserved). REMAINING (Phase 3): 15+19 purchase-frontend (kill "Buy at Google Shopping" → /api/orders), 18 geo Browse, 23 stories-frontend (Sam backend ready). KNOWN DEBT: comments endpoint still in-memory (BE-005 gap, flagged by Sam+Shira). |
| 2026-06-30 | jeff | main / static/index.html | done | Phase 3 wave SHIPPED & PUSHED (origin/main 7f792de) — completes the Carmel/Razi 24-item backlog. 3 parallel worktree agents, 0 conflicts: Oren in-app Buy via /api/orders + removed all external "Google Shopping" wording/redirect (grep "google shopping"=0; preloved kind→8% commission, retail→0; client_ref idempotency) (b138c20); Shira real 24h ephemeral outfit stories wired to /api/stories + full-screen viewer w/ progress bars/auto-advance/owner-delete (0fe5377); Valentino local/radius "Near me" filter for pre-loved (geolocation opt-in + haversine + seed coords, graceful denial) (ed0efc0). Verified: node --check + Playwright (0 JS errors, 0 internal 404s after cleaning agent curl-test rows from dev DB — stories/daily_logs/orders test rows purged). KNOWN DEBT carried: comments endpoint still in-memory (BE-005), seller_key=@handle not canonical user-id (BE-003), in-app order confirmation is optimistic not gated on 200 (flagged by Oren). |
| 2026-07-05 | scout | docs/research | done | First intelligence run: Whering teardown |
| 2026-07-05 | claude (main session) | infra: autopilot.yml, guard_checks.sh, hooks, telegram*, roles | done | Mechanism hardening per 6-layer audit: workflow-enforced verify gate before push (OW-006), fast-heal cooldown bypass, disabled duplicate local launchd loop, INDEX backfill (6 codes) + sync gate, SessionStart knowledge-inject + PreToolUse Iron-Rule guard hooks, Telegram fail-closed auth + honest acks + retry, @agent now fires managers run, MASTER_PLAN-strategy reconciliation, Shira re-parent (tech=Steve), uniform DoD, BE-006 (ex-MG-005 user_key), loop-liveness watchdog |
| 2026-07-05 | claude (main session) | main/scripts/tglib.py + workflows + docs | הושלם | ביקורת-תשתית מלאה + מימוש: שכבת טלגרם קנונית (tglib — chunking/retry/audit-log, offset persistence, INBOX flock), engine עבר ל-auto/engine דרך שער jeff-merge (ביקורת פרסונות Gabbana/Steve), strategy.yml אוטונומי (חידות 05-08), אכיפת LEARNING_LOG ב-retrospective, מחיקת זומבים (orchestrator/autopilot.sh/agents ישנה/docs-MASTER_PLAN/מסמכי עיצוב ישנים), עדכון CLAUDE.md/MASTER_PLAN/plans-INDEX/spa-navigation למציאות |
| 2026-07-05 | sam | auto/steve / app.py + tests/test_app.py | done | P0 fix: Creator Wallet always $0 — GET /api/wallet now accepts ?user_id= matching credits.user_key=influencer_id written by POST /api/orders; balance sums full ledger (was LIMIT-50 undercount); rate limit stays IP-keyed (BE-006); 3 new pytests, fail-before/pass-after proven, 22/22 green |
| 2026-07-05 | netta (mark lane) | auto/mark / static/app.css | done | Killed last dark-theme relics: .adm-grade-card/.sus-score-card near-black gradients → var(--success-surface)+color-mix borders, AA ink (10.5:1) on mint per gabbana P0, body/shelf-ledge bare hex tokenized; gabbana gate 9/10 PASS; bare hex outside :root = 0 |

# Mobile workspace

עריכה: וראן (ראש), דנה, רועי. כולם אחרים — קריאה בלבד.

כל proposal: תיקייה משלו `workspace/mobile/<task-name>/` עם README (מה/למה/diff/אימות). וראן מושך, מבקר, ואם מאשר — מקדם ל-`workspace/board/mobile/`.

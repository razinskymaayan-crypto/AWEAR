# Design workspace

עריכה: מארק (ראש), דולצ'ה, גבאנה, נטה. כולם אחרים — קריאה בלבד.

כל proposal: תיקייה משלו `workspace/design/<task-name>/` עם README (מה/למה/diff/אימות Playwright). מארק מושך, מבקר, ואם מאשר — מקדם ל-`workspace/board/design/`.

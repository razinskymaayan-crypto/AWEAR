# Backend workspace

עריכה: סטיב (ראש), סאם, אורן. כולם אחרים — קריאה בלבד.

כל proposal: תיקייה משלו `workspace/backend/<task-name>/` עם README (מה/למה/diff/אימות). סטיב מושך, מבקר, ואם מאשר — מקדם ל-`workspace/board/backend/`.

# Daily Digest — 2026-06-28

- Done: Oren shipped backend integration hardening — /api/dm/send now rejects unknown recipients (404) so the DM store never accrues orphaned ghost threads (fail-open on empty cache). Verified with FastAPI TestClient. | Proposed: nothing new this run. | Blocked: a future "Message seller" link from Community store pages needs an ID-namespace reconciliation (Community u1-style ids vs DM user_NNN) — flagged by steve as a separate larger task, not attempted.
- Done: Dolce fixed the broken "Today's Look" hero on Home (the demo's first screen) — a blank white tile + a random street/metal-sculpture photo were showing because seed items had dead/missing image URLs (loremflickr random fallback). Per Mark's direction, bundled all 13 demo closet images locally in static/img/closet/ (off live CDNs → zero rot risk for the live demo, A6). Gabbana 8.5 PASS; post-fix survey (ayalon, n=100) 8/10 up from ~3 broken, no severe. commit 27ef210. | Proposed (IDEAS.md #13-17): hero crop/lighting consistency + de-dupe denim + copy truncation + button glow + extend local-bundle to feed/marketplace seeds. | Blocked: NONE for this task — but the agent session is write-gated on the .claude/ tree, so the activity_log.md + contributions ledger entries for this run could not be written by the agent (see TODO_FOR_TAMAR). Entry text is preserved in the commit message + this digest.
  - Activity-log entry (for manual paste): | 2026-06-28 | dolce | main / static/index.html + static/img/closet/ | done | Fixed broken Today's Look hero on Home; bundled 13 demo closet images locally (off CDNs); broadened _productImgUrl guard for local /-paths; Gabbana 8.5 PASS; survey 8/10. commit 27ef210. |
  - Ledger (contributions/2026-06-28.md): dolce(mark,~110k) image fix; mark(mark,~18k) off-CDN direction; gabbana(mark,~28k) 8.5 gate; ayalon(ayalon,~22k) n=100 confirmation survey.
- Done: Dolce fixed two visible defects on the Profile screen (closet view — the investor "wardrobe is the profile" thesis screen), an idle-agent task routed via Mark. (1) The user's bio was invisible in light mode — rendered with hardcoded near-white rgba(246,246,249,.7) (DS-004 violation); moved to a new .ig-name-bio class using var(--muted). (2) The empty-avatar state showed a blank gray circle — the surface-fill ::after was painting over the user fallback icon; fixed z-index stacking (fill 0 → icon 1 → photo 2) and consolidated two conflicting .ava-ico rules. Gabbana 8.5 PASS (both hold in light + dark). check-render EXIT 0. | Proposed (IDEAS #18-19): systemic light-mode contrast sweep — ~9 other elements still use hardcoded near-white rgba and are invisible/low-contrast on cream bg; + darken --muted toward WCAG AA. | Blocked: NONE. Note: .claude/ tree is write-gated this session, so activity_log + contributions ledger entries are preserved below for manual paste.
  - Activity-log entry (for manual paste): | 2026-06-28 | dolce | main / static/index.html | done | Profile-header (closet) 2 fixes via mark: invisible bio rgba(246,246,249,.7)→.ig-name-bio var(--muted); empty-avatar fallback icon z-index fix (::after z0/.ava-ico z1/-photo z2, consolidated 2 conflicting rules). Gabbana 8.5 PASS, check-render OK. IDEAS #18-19 systemic light-mode contrast. |
  - Ledger (contributions/2026-06-28.md): mark(mark,~16k) direction; dolce(mark,~36k) implemented 2 fixes; gabbana(mark,~20k) 8.5 gate.
- Done: Dolce gave the Profile/Closet hero a branded identity. The avatar no longer falls back to a generic gray person glyph when the user has no photo — it now renders a gradient initials-monogram ("Carmel Pikarsky" → a "CP" circle, dark/charcoal in light mode matching the dark buttons, terracotta→camel in dark mode), the way Instagram/Pinterest do. This strengthens the "wardrobe is the profile" identity hero — the demo's first impression of a user. Idle-agent task (dolce idle 9 days) routed via Mark → implemented → Gabbana 8/10 ship. Two QA catches closed inline: (1) dolce first used var(--text) for the monogram, which flips to near-black in light mode = dark-on-dark invisible → switched to var(--on-accent,#fff); (2) Netta then defined --on-accent (a phantom token referenced in ~8 components but never declared) across tokens.css + both awear-tokens.json copies; (3) added a text-shadow for dark-mode camel-edge WCAG AA. check-render EXIT 0. | Proposed (IDEAS): fix the two divergent awear-tokens.json sources (root=old palette, static=v2) + the missing Style-Dictionary build (CSS/JSON hand-mirrored today). | Blocked: NONE. Note: .claude/ tree write-gated this session → activity_log + ledger entries preserved in the commit message for manual paste.
  - Activity-log entry (for manual paste): | 2026-06-28 | dolce | main/static/index.html | done | Profile/Closet hero: gradient initials-monogram avatar fallback (via mark). prof.photo→initials("CP")→icon('user') chain; promoted initials() to module scope; var(--on-accent,#fff) not --text (light-mode contrast); dark-mode text-shadow. Gabbana 8/10, check-render OK. |  AND  | 2026-06-28 | netta | main/static/tokens.css+awear-tokens.json | done | Defined phantom --on-accent:#fff across CSS + both JSON; surfaced JSON-divergence + no-regen-build debt. |
  - Ledger (contributions/2026-06-28.md): mark(mark,~22k) picked + scoped the idle-dolce win; dolce(mark,~103k) implemented monogram + 2 QA fixes; gabbana(mark,~21k) 8/10 gate; netta(mark,~61k) defined --on-accent across 3 token sources.
